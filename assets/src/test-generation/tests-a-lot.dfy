include "/Users/fedchina/Desktop/blog/assets/src/test-generation/chessWithQuantifiers.dfy"
module UsersfedchinaDesktopblogassetssrctestgenerationchessWithQuantifiersdfyUnitTests {
import Chess
method {:test} Test0() {
var color0 : Chess.Color := Chess.Color.White;
var kind0 : Chess.Kind := Chess.Kind.King(c:=color0);
var chessPos0 : Chess.ChessPos := Chess.Pos.Pos(row:=6,col:=7);
expect 0 <= chessPos0.row < 8 && 0 <= chessPos0.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece0 : Chess.Piece := Chess.Piece.Piece(kind:=kind0,at:=chessPos0);
var color1 : Chess.Color := Chess.Color.Black;
var kind1 : Chess.Kind := Chess.Kind.Knight(c:=color1);
var chessPos1 : Chess.ChessPos := Chess.Pos.Pos(row:=4,col:=3);
expect 0 <= chessPos1.row < 8 && 0 <= chessPos1.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece1 : Chess.Piece := Chess.Piece.Piece(kind:=kind1,at:=chessPos1);
var color2 : Chess.Color := Chess.Color.Black;
var kind2 : Chess.Kind := Chess.Kind.Knight(c:=color2);
var chessPos2 : Chess.ChessPos := Chess.Pos.Pos(row:=2,col:=2);
expect 0 <= chessPos2.row < 8 && 0 <= chessPos2.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece2 : Chess.Piece := Chess.Piece.Piece(kind:=kind2,at:=chessPos2);
var color3 : Chess.Color := Chess.Color.Black;
var kind3 : Chess.Kind := Chess.Kind.Pawn(c:=color3);
var chessPos3 : Chess.ChessPos := Chess.Pos.Pos(row:=1,col:=1);
expect 0 <= chessPos3.row < 8 && 0 <= chessPos3.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece3 : Chess.Piece := Chess.Piece.Piece(kind:=kind3,at:=chessPos3);
var color4 : Chess.Color := Chess.Color.Black;
var kind4 : Chess.Kind := Chess.Kind.Pawn(c:=color4);
var chessPos4 : Chess.ChessPos := Chess.Pos.Pos(row:=0,col:=0);
expect 0 <= chessPos4.row < 8 && 0 <= chessPos4.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece4 : Chess.Piece := Chess.Piece.Piece(kind:=kind4,at:=chessPos4);
var piece5 : seq<Chess.Piece> := [piece0, piece1, piece2, piece3, piece4];
var validBoard0 : Chess.ValidBoard := Chess.Board.Board(pieces:=piece5);
expect Chess.BoardIsValid(validBoard0), "If this check fails at runtime, the test does not meet the type constraints";
expect Chess.BoardPreset(validBoard0), "If this check fails at runtime, the test does not meet the preconditions";
Chess.Describe(validBoard0);
}
method {:test} Test1() {
var color0 : Chess.Color := Chess.Color.White;
var kind0 : Chess.Kind := Chess.Kind.King(c:=color0);
var chessPos0 : Chess.ChessPos := Chess.Pos.Pos(row:=3,col:=6);
expect 0 <= chessPos0.row < 8 && 0 <= chessPos0.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece0 : Chess.Piece := Chess.Piece.Piece(kind:=kind0,at:=chessPos0);
var color1 : Chess.Color := Chess.Color.Black;
var kind1 : Chess.Kind := Chess.Kind.Knight(c:=color1);
var chessPos1 : Chess.ChessPos := Chess.Pos.Pos(row:=3,col:=7);
expect 0 <= chessPos1.row < 8 && 0 <= chessPos1.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece1 : Chess.Piece := Chess.Piece.Piece(kind:=kind1,at:=chessPos1);
var color2 : Chess.Color := Chess.Color.Black;
var kind2 : Chess.Kind := Chess.Kind.Knight(c:=color2);
var chessPos2 : Chess.ChessPos := Chess.Pos.Pos(row:=2,col:=4);
expect 0 <= chessPos2.row < 8 && 0 <= chessPos2.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece2 : Chess.Piece := Chess.Piece.Piece(kind:=kind2,at:=chessPos2);
var color3 : Chess.Color := Chess.Color.Black;
var kind3 : Chess.Kind := Chess.Kind.Pawn(c:=color3);
var chessPos3 : Chess.ChessPos := Chess.Pos.Pos(row:=1,col:=1);
expect 0 <= chessPos3.row < 8 && 0 <= chessPos3.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece3 : Chess.Piece := Chess.Piece.Piece(kind:=kind3,at:=chessPos3);
var color4 : Chess.Color := Chess.Color.Black;
var kind4 : Chess.Kind := Chess.Kind.Pawn(c:=color4);
var chessPos4 : Chess.ChessPos := Chess.Pos.Pos(row:=0,col:=0);
expect 0 <= chessPos4.row < 8 && 0 <= chessPos4.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece4 : Chess.Piece := Chess.Piece.Piece(kind:=kind4,at:=chessPos4);
var piece5 : seq<Chess.Piece> := [piece0, piece1, piece2, piece3, piece4];
var validBoard0 : Chess.ValidBoard := Chess.Board.Board(pieces:=piece5);
expect Chess.BoardIsValid(validBoard0), "If this check fails at runtime, the test does not meet the type constraints";
expect Chess.BoardPreset(validBoard0), "If this check fails at runtime, the test does not meet the preconditions";
Chess.Describe(validBoard0);
}
method {:test} Test2() {
var color0 : Chess.Color := Chess.Color.White;
var kind0 : Chess.Kind := Chess.Kind.King(c:=color0);
var chessPos0 : Chess.ChessPos := Chess.Pos.Pos(row:=6,col:=6);
expect 0 <= chessPos0.row < 8 && 0 <= chessPos0.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece0 : Chess.Piece := Chess.Piece.Piece(kind:=kind0,at:=chessPos0);
var color1 : Chess.Color := Chess.Color.Black;
var kind1 : Chess.Kind := Chess.Kind.Knight(c:=color1);
var chessPos1 : Chess.ChessPos := Chess.Pos.Pos(row:=4,col:=5);
expect 0 <= chessPos1.row < 8 && 0 <= chessPos1.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece1 : Chess.Piece := Chess.Piece.Piece(kind:=kind1,at:=chessPos1);
var color2 : Chess.Color := Chess.Color.Black;
var kind2 : Chess.Kind := Chess.Kind.Knight(c:=color2);
var chessPos2 : Chess.ChessPos := Chess.Pos.Pos(row:=5,col:=4);
expect 0 <= chessPos2.row < 8 && 0 <= chessPos2.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece2 : Chess.Piece := Chess.Piece.Piece(kind:=kind2,at:=chessPos2);
var color3 : Chess.Color := Chess.Color.Black;
var kind3 : Chess.Kind := Chess.Kind.Pawn(c:=color3);
var chessPos3 : Chess.ChessPos := Chess.Pos.Pos(row:=1,col:=2);
expect 0 <= chessPos3.row < 8 && 0 <= chessPos3.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece3 : Chess.Piece := Chess.Piece.Piece(kind:=kind3,at:=chessPos3);
var color4 : Chess.Color := Chess.Color.Black;
var kind4 : Chess.Kind := Chess.Kind.Pawn(c:=color4);
var chessPos4 : Chess.ChessPos := Chess.Pos.Pos(row:=0,col:=1);
expect 0 <= chessPos4.row < 8 && 0 <= chessPos4.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece4 : Chess.Piece := Chess.Piece.Piece(kind:=kind4,at:=chessPos4);
var piece5 : seq<Chess.Piece> := [piece0, piece1, piece2, piece3, piece4];
var validBoard0 : Chess.ValidBoard := Chess.Board.Board(pieces:=piece5);
expect Chess.BoardIsValid(validBoard0), "If this check fails at runtime, the test does not meet the type constraints";
expect Chess.BoardPreset(validBoard0), "If this check fails at runtime, the test does not meet the preconditions";
Chess.Describe(validBoard0);
}
method {:test} Test3() {
var color0 : Chess.Color := Chess.Color.White;
var kind0 : Chess.Kind := Chess.Kind.King(c:=color0);
var chessPos0 : Chess.ChessPos := Chess.Pos.Pos(row:=7,col:=7);
expect 0 <= chessPos0.row < 8 && 0 <= chessPos0.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece0 : Chess.Piece := Chess.Piece.Piece(kind:=kind0,at:=chessPos0);
var color1 : Chess.Color := Chess.Color.Black;
var kind1 : Chess.Kind := Chess.Kind.Knight(c:=color1);
var chessPos1 : Chess.ChessPos := Chess.Pos.Pos(row:=6,col:=4);
expect 0 <= chessPos1.row < 8 && 0 <= chessPos1.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece1 : Chess.Piece := Chess.Piece.Piece(kind:=kind1,at:=chessPos1);
var color2 : Chess.Color := Chess.Color.Black;
var kind2 : Chess.Kind := Chess.Kind.Knight(c:=color2);
var chessPos2 : Chess.ChessPos := Chess.Pos.Pos(row:=4,col:=3);
expect 0 <= chessPos2.row < 8 && 0 <= chessPos2.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece2 : Chess.Piece := Chess.Piece.Piece(kind:=kind2,at:=chessPos2);
var color3 : Chess.Color := Chess.Color.Black;
var kind3 : Chess.Kind := Chess.Kind.Pawn(c:=color3);
var chessPos3 : Chess.ChessPos := Chess.Pos.Pos(row:=1,col:=1);
expect 0 <= chessPos3.row < 8 && 0 <= chessPos3.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece3 : Chess.Piece := Chess.Piece.Piece(kind:=kind3,at:=chessPos3);
var color4 : Chess.Color := Chess.Color.Black;
var kind4 : Chess.Kind := Chess.Kind.Pawn(c:=color4);
var chessPos4 : Chess.ChessPos := Chess.Pos.Pos(row:=0,col:=0);
expect 0 <= chessPos4.row < 8 && 0 <= chessPos4.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece4 : Chess.Piece := Chess.Piece.Piece(kind:=kind4,at:=chessPos4);
var piece5 : seq<Chess.Piece> := [piece0, piece1, piece2, piece3, piece4];
var validBoard0 : Chess.ValidBoard := Chess.Board.Board(pieces:=piece5);
expect Chess.BoardIsValid(validBoard0), "If this check fails at runtime, the test does not meet the type constraints";
expect Chess.BoardPreset(validBoard0), "If this check fails at runtime, the test does not meet the preconditions";
Chess.Describe(validBoard0);
}
method {:test} Test4() {
var color0 : Chess.Color := Chess.Color.White;
var kind0 : Chess.Kind := Chess.Kind.King(c:=color0);
var chessPos0 : Chess.ChessPos := Chess.Pos.Pos(row:=5,col:=6);
expect 0 <= chessPos0.row < 8 && 0 <= chessPos0.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece0 : Chess.Piece := Chess.Piece.Piece(kind:=kind0,at:=chessPos0);
var color1 : Chess.Color := Chess.Color.Black;
var kind1 : Chess.Kind := Chess.Kind.Knight(c:=color1);
var chessPos1 : Chess.ChessPos := Chess.Pos.Pos(row:=4,col:=2);
expect 0 <= chessPos1.row < 8 && 0 <= chessPos1.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece1 : Chess.Piece := Chess.Piece.Piece(kind:=kind1,at:=chessPos1);
var color2 : Chess.Color := Chess.Color.Black;
var kind2 : Chess.Kind := Chess.Kind.Knight(c:=color2);
var chessPos2 : Chess.ChessPos := Chess.Pos.Pos(row:=3,col:=1);
expect 0 <= chessPos2.row < 8 && 0 <= chessPos2.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece2 : Chess.Piece := Chess.Piece.Piece(kind:=kind2,at:=chessPos2);
var color3 : Chess.Color := Chess.Color.Black;
var kind3 : Chess.Kind := Chess.Kind.Pawn(c:=color3);
var chessPos3 : Chess.ChessPos := Chess.Pos.Pos(row:=7,col:=7);
expect 0 <= chessPos3.row < 8 && 0 <= chessPos3.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece3 : Chess.Piece := Chess.Piece.Piece(kind:=kind3,at:=chessPos3);
var color4 : Chess.Color := Chess.Color.Black;
var kind4 : Chess.Kind := Chess.Kind.Pawn(c:=color4);
var chessPos4 : Chess.ChessPos := Chess.Pos.Pos(row:=2,col:=0);
expect 0 <= chessPos4.row < 8 && 0 <= chessPos4.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece4 : Chess.Piece := Chess.Piece.Piece(kind:=kind4,at:=chessPos4);
var piece5 : seq<Chess.Piece> := [piece0, piece1, piece2, piece3, piece4];
var validBoard0 : Chess.ValidBoard := Chess.Board.Board(pieces:=piece5);
expect Chess.BoardIsValid(validBoard0), "If this check fails at runtime, the test does not meet the type constraints";
expect Chess.BoardPreset(validBoard0), "If this check fails at runtime, the test does not meet the preconditions";
Chess.Describe(validBoard0);
}
method {:test} Test5() {
var color0 : Chess.Color := Chess.Color.White;
var kind0 : Chess.Kind := Chess.Kind.King(c:=color0);
var chessPos0 : Chess.ChessPos := Chess.Pos.Pos(row:=3,col:=4);
expect 0 <= chessPos0.row < 8 && 0 <= chessPos0.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece0 : Chess.Piece := Chess.Piece.Piece(kind:=kind0,at:=chessPos0);
var color1 : Chess.Color := Chess.Color.Black;
var kind1 : Chess.Kind := Chess.Kind.Knight(c:=color1);
var chessPos1 : Chess.ChessPos := Chess.Pos.Pos(row:=7,col:=7);
expect 0 <= chessPos1.row < 8 && 0 <= chessPos1.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece1 : Chess.Piece := Chess.Piece.Piece(kind:=kind1,at:=chessPos1);
var color2 : Chess.Color := Chess.Color.Black;
var kind2 : Chess.Kind := Chess.Kind.Knight(c:=color2);
var chessPos2 : Chess.ChessPos := Chess.Pos.Pos(row:=1,col:=1);
expect 0 <= chessPos2.row < 8 && 0 <= chessPos2.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece2 : Chess.Piece := Chess.Piece.Piece(kind:=kind2,at:=chessPos2);
var color3 : Chess.Color := Chess.Color.Black;
var kind3 : Chess.Kind := Chess.Kind.Pawn(c:=color3);
var chessPos3 : Chess.ChessPos := Chess.Pos.Pos(row:=4,col:=6);
expect 0 <= chessPos3.row < 8 && 0 <= chessPos3.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece3 : Chess.Piece := Chess.Piece.Piece(kind:=kind3,at:=chessPos3);
var color4 : Chess.Color := Chess.Color.Black;
var kind4 : Chess.Kind := Chess.Kind.Pawn(c:=color4);
var chessPos4 : Chess.ChessPos := Chess.Pos.Pos(row:=0,col:=0);
expect 0 <= chessPos4.row < 8 && 0 <= chessPos4.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece4 : Chess.Piece := Chess.Piece.Piece(kind:=kind4,at:=chessPos4);
var piece5 : seq<Chess.Piece> := [piece0, piece1, piece2, piece3, piece4];
var validBoard0 : Chess.ValidBoard := Chess.Board.Board(pieces:=piece5);
expect Chess.BoardIsValid(validBoard0), "If this check fails at runtime, the test does not meet the type constraints";
expect Chess.BoardPreset(validBoard0), "If this check fails at runtime, the test does not meet the preconditions";
Chess.Describe(validBoard0);
}
method {:test} Test6() {
var color0 : Chess.Color := Chess.Color.White;
var kind0 : Chess.Kind := Chess.Kind.King(c:=color0);
var chessPos0 : Chess.ChessPos := Chess.Pos.Pos(row:=4,col:=5);
expect 0 <= chessPos0.row < 8 && 0 <= chessPos0.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece0 : Chess.Piece := Chess.Piece.Piece(kind:=kind0,at:=chessPos0);
var color1 : Chess.Color := Chess.Color.Black;
var kind1 : Chess.Kind := Chess.Kind.Knight(c:=color1);
var chessPos1 : Chess.ChessPos := Chess.Pos.Pos(row:=2,col:=2);
expect 0 <= chessPos1.row < 8 && 0 <= chessPos1.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece1 : Chess.Piece := Chess.Piece.Piece(kind:=kind1,at:=chessPos1);
var color2 : Chess.Color := Chess.Color.Black;
var kind2 : Chess.Kind := Chess.Kind.Knight(c:=color2);
var chessPos2 : Chess.ChessPos := Chess.Pos.Pos(row:=1,col:=1);
expect 0 <= chessPos2.row < 8 && 0 <= chessPos2.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece2 : Chess.Piece := Chess.Piece.Piece(kind:=kind2,at:=chessPos2);
var color3 : Chess.Color := Chess.Color.Black;
var kind3 : Chess.Kind := Chess.Kind.Pawn(c:=color3);
var chessPos3 : Chess.ChessPos := Chess.Pos.Pos(row:=6,col:=6);
expect 0 <= chessPos3.row < 8 && 0 <= chessPos3.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece3 : Chess.Piece := Chess.Piece.Piece(kind:=kind3,at:=chessPos3);
var color4 : Chess.Color := Chess.Color.Black;
var kind4 : Chess.Kind := Chess.Kind.Pawn(c:=color4);
var chessPos4 : Chess.ChessPos := Chess.Pos.Pos(row:=0,col:=0);
expect 0 <= chessPos4.row < 8 && 0 <= chessPos4.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece4 : Chess.Piece := Chess.Piece.Piece(kind:=kind4,at:=chessPos4);
var piece5 : seq<Chess.Piece> := [piece0, piece1, piece2, piece3, piece4];
var validBoard0 : Chess.ValidBoard := Chess.Board.Board(pieces:=piece5);
expect Chess.BoardIsValid(validBoard0), "If this check fails at runtime, the test does not meet the type constraints";
expect Chess.BoardPreset(validBoard0), "If this check fails at runtime, the test does not meet the preconditions";
Chess.Describe(validBoard0);
}
method {:test} Test7() {
var color0 : Chess.Color := Chess.Color.White;
var kind0 : Chess.Kind := Chess.Kind.King(c:=color0);
var chessPos0 : Chess.ChessPos := Chess.Pos.Pos(row:=2,col:=4);
expect 0 <= chessPos0.row < 8 && 0 <= chessPos0.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece0 : Chess.Piece := Chess.Piece.Piece(kind:=kind0,at:=chessPos0);
var color1 : Chess.Color := Chess.Color.Black;
var kind1 : Chess.Kind := Chess.Kind.Knight(c:=color1);
var chessPos1 : Chess.ChessPos := Chess.Pos.Pos(row:=6,col:=7);
expect 0 <= chessPos1.row < 8 && 0 <= chessPos1.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece1 : Chess.Piece := Chess.Piece.Piece(kind:=kind1,at:=chessPos1);
var color2 : Chess.Color := Chess.Color.Black;
var kind2 : Chess.Kind := Chess.Kind.Knight(c:=color2);
var chessPos2 : Chess.ChessPos := Chess.Pos.Pos(row:=1,col:=1);
expect 0 <= chessPos2.row < 8 && 0 <= chessPos2.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece2 : Chess.Piece := Chess.Piece.Piece(kind:=kind2,at:=chessPos2);
var color3 : Chess.Color := Chess.Color.Black;
var kind3 : Chess.Kind := Chess.Kind.Pawn(c:=color3);
var chessPos3 : Chess.ChessPos := Chess.Pos.Pos(row:=3,col:=5);
expect 0 <= chessPos3.row < 8 && 0 <= chessPos3.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece3 : Chess.Piece := Chess.Piece.Piece(kind:=kind3,at:=chessPos3);
var color4 : Chess.Color := Chess.Color.Black;
var kind4 : Chess.Kind := Chess.Kind.Pawn(c:=color4);
var chessPos4 : Chess.ChessPos := Chess.Pos.Pos(row:=0,col:=0);
expect 0 <= chessPos4.row < 8 && 0 <= chessPos4.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece4 : Chess.Piece := Chess.Piece.Piece(kind:=kind4,at:=chessPos4);
var piece5 : seq<Chess.Piece> := [piece0, piece1, piece2, piece3, piece4];
var validBoard0 : Chess.ValidBoard := Chess.Board.Board(pieces:=piece5);
expect Chess.BoardIsValid(validBoard0), "If this check fails at runtime, the test does not meet the type constraints";
expect Chess.BoardPreset(validBoard0), "If this check fails at runtime, the test does not meet the preconditions";
Chess.Describe(validBoard0);
}
method {:test} Test8() {
var color0 : Chess.Color := Chess.Color.White;
var kind0 : Chess.Kind := Chess.Kind.King(c:=color0);
var chessPos0 : Chess.ChessPos := Chess.Pos.Pos(row:=7,col:=7);
expect 0 <= chessPos0.row < 8 && 0 <= chessPos0.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece0 : Chess.Piece := Chess.Piece.Piece(kind:=kind0,at:=chessPos0);
var color1 : Chess.Color := Chess.Color.Black;
var kind1 : Chess.Kind := Chess.Kind.Knight(c:=color1);
var chessPos1 : Chess.ChessPos := Chess.Pos.Pos(row:=5,col:=4);
expect 0 <= chessPos1.row < 8 && 0 <= chessPos1.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece1 : Chess.Piece := Chess.Piece.Piece(kind:=kind1,at:=chessPos1);
var color2 : Chess.Color := Chess.Color.Black;
var kind2 : Chess.Kind := Chess.Kind.Knight(c:=color2);
var chessPos2 : Chess.ChessPos := Chess.Pos.Pos(row:=1,col:=3);
expect 0 <= chessPos2.row < 8 && 0 <= chessPos2.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece2 : Chess.Piece := Chess.Piece.Piece(kind:=kind2,at:=chessPos2);
var color3 : Chess.Color := Chess.Color.Black;
var kind3 : Chess.Kind := Chess.Kind.Pawn(c:=color3);
var chessPos3 : Chess.ChessPos := Chess.Pos.Pos(row:=6,col:=1);
expect 0 <= chessPos3.row < 8 && 0 <= chessPos3.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece3 : Chess.Piece := Chess.Piece.Piece(kind:=kind3,at:=chessPos3);
var color4 : Chess.Color := Chess.Color.Black;
var kind4 : Chess.Kind := Chess.Kind.Pawn(c:=color4);
var chessPos4 : Chess.ChessPos := Chess.Pos.Pos(row:=0,col:=0);
expect 0 <= chessPos4.row < 8 && 0 <= chessPos4.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece4 : Chess.Piece := Chess.Piece.Piece(kind:=kind4,at:=chessPos4);
var piece5 : seq<Chess.Piece> := [piece0, piece1, piece2, piece3, piece4];
var validBoard0 : Chess.ValidBoard := Chess.Board.Board(pieces:=piece5);
expect Chess.BoardIsValid(validBoard0), "If this check fails at runtime, the test does not meet the type constraints";
expect Chess.BoardPreset(validBoard0), "If this check fails at runtime, the test does not meet the preconditions";
Chess.Describe(validBoard0);
}
method {:test} Test9() {
var color0 : Chess.Color := Chess.Color.White;
var kind0 : Chess.Kind := Chess.Kind.King(c:=color0);
var chessPos0 : Chess.ChessPos := Chess.Pos.Pos(row:=7,col:=5);
expect 0 <= chessPos0.row < 8 && 0 <= chessPos0.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece0 : Chess.Piece := Chess.Piece.Piece(kind:=kind0,at:=chessPos0);
var color1 : Chess.Color := Chess.Color.Black;
var kind1 : Chess.Kind := Chess.Kind.Knight(c:=color1);
var chessPos1 : Chess.ChessPos := Chess.Pos.Pos(row:=2,col:=1);
expect 0 <= chessPos1.row < 8 && 0 <= chessPos1.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece1 : Chess.Piece := Chess.Piece.Piece(kind:=kind1,at:=chessPos1);
var color2 : Chess.Color := Chess.Color.Black;
var kind2 : Chess.Kind := Chess.Kind.Knight(c:=color2);
var chessPos2 : Chess.ChessPos := Chess.Pos.Pos(row:=1,col:=0);
expect 0 <= chessPos2.row < 8 && 0 <= chessPos2.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece2 : Chess.Piece := Chess.Piece.Piece(kind:=kind2,at:=chessPos2);
var color3 : Chess.Color := Chess.Color.Black;
var kind3 : Chess.Kind := Chess.Kind.Pawn(c:=color3);
var chessPos3 : Chess.ChessPos := Chess.Pos.Pos(row:=6,col:=4);
expect 0 <= chessPos3.row < 8 && 0 <= chessPos3.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece3 : Chess.Piece := Chess.Piece.Piece(kind:=kind3,at:=chessPos3);
var color4 : Chess.Color := Chess.Color.Black;
var kind4 : Chess.Kind := Chess.Kind.Pawn(c:=color4);
var chessPos4 : Chess.ChessPos := Chess.Pos.Pos(row:=0,col:=3);
expect 0 <= chessPos4.row < 8 && 0 <= chessPos4.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece4 : Chess.Piece := Chess.Piece.Piece(kind:=kind4,at:=chessPos4);
var piece5 : seq<Chess.Piece> := [piece0, piece1, piece2, piece3, piece4];
var validBoard0 : Chess.ValidBoard := Chess.Board.Board(pieces:=piece5);
expect Chess.BoardIsValid(validBoard0), "If this check fails at runtime, the test does not meet the type constraints";
expect Chess.BoardPreset(validBoard0), "If this check fails at runtime, the test does not meet the preconditions";
Chess.Describe(validBoard0);
}
method {:test} Test10() {
var color0 : Chess.Color := Chess.Color.White;
var kind0 : Chess.Kind := Chess.Kind.King(c:=color0);
var chessPos0 : Chess.ChessPos := Chess.Pos.Pos(row:=5,col:=5);
expect 0 <= chessPos0.row < 8 && 0 <= chessPos0.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece0 : Chess.Piece := Chess.Piece.Piece(kind:=kind0,at:=chessPos0);
var color1 : Chess.Color := Chess.Color.Black;
var kind1 : Chess.Kind := Chess.Kind.Knight(c:=color1);
var chessPos1 : Chess.ChessPos := Chess.Pos.Pos(row:=4,col:=7);
expect 0 <= chessPos1.row < 8 && 0 <= chessPos1.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece1 : Chess.Piece := Chess.Piece.Piece(kind:=kind1,at:=chessPos1);
var color2 : Chess.Color := Chess.Color.Black;
var kind2 : Chess.Kind := Chess.Kind.Knight(c:=color2);
var chessPos2 : Chess.ChessPos := Chess.Pos.Pos(row:=4,col:=3);
expect 0 <= chessPos2.row < 8 && 0 <= chessPos2.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece2 : Chess.Piece := Chess.Piece.Piece(kind:=kind2,at:=chessPos2);
var color3 : Chess.Color := Chess.Color.Black;
var kind3 : Chess.Kind := Chess.Kind.Pawn(c:=color3);
var chessPos3 : Chess.ChessPos := Chess.Pos.Pos(row:=3,col:=1);
expect 0 <= chessPos3.row < 8 && 0 <= chessPos3.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece3 : Chess.Piece := Chess.Piece.Piece(kind:=kind3,at:=chessPos3);
var color4 : Chess.Color := Chess.Color.Black;
var kind4 : Chess.Kind := Chess.Kind.Pawn(c:=color4);
var chessPos4 : Chess.ChessPos := Chess.Pos.Pos(row:=2,col:=0);
expect 0 <= chessPos4.row < 8 && 0 <= chessPos4.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece4 : Chess.Piece := Chess.Piece.Piece(kind:=kind4,at:=chessPos4);
var piece5 : seq<Chess.Piece> := [piece0, piece1, piece2, piece3, piece4];
var validBoard0 : Chess.ValidBoard := Chess.Board.Board(pieces:=piece5);
expect Chess.BoardIsValid(validBoard0), "If this check fails at runtime, the test does not meet the type constraints";
expect Chess.BoardPreset(validBoard0), "If this check fails at runtime, the test does not meet the preconditions";
Chess.Describe(validBoard0);
}
method {:test} Test11() {
var color0 : Chess.Color := Chess.Color.White;
var kind0 : Chess.Kind := Chess.Kind.King(c:=color0);
var chessPos0 : Chess.ChessPos := Chess.Pos.Pos(row:=4,col:=5);
expect 0 <= chessPos0.row < 8 && 0 <= chessPos0.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece0 : Chess.Piece := Chess.Piece.Piece(kind:=kind0,at:=chessPos0);
var color1 : Chess.Color := Chess.Color.Black;
var kind1 : Chess.Kind := Chess.Kind.Knight(c:=color1);
var chessPos1 : Chess.ChessPos := Chess.Pos.Pos(row:=2,col:=4);
expect 0 <= chessPos1.row < 8 && 0 <= chessPos1.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece1 : Chess.Piece := Chess.Piece.Piece(kind:=kind1,at:=chessPos1);
var color2 : Chess.Color := Chess.Color.Black;
var kind2 : Chess.Kind := Chess.Kind.Knight(c:=color2);
var chessPos2 : Chess.ChessPos := Chess.Pos.Pos(row:=3,col:=3);
expect 0 <= chessPos2.row < 8 && 0 <= chessPos2.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece2 : Chess.Piece := Chess.Piece.Piece(kind:=kind2,at:=chessPos2);
var color3 : Chess.Color := Chess.Color.Black;
var kind3 : Chess.Kind := Chess.Kind.Pawn(c:=color3);
var chessPos3 : Chess.ChessPos := Chess.Pos.Pos(row:=1,col:=1);
expect 0 <= chessPos3.row < 8 && 0 <= chessPos3.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece3 : Chess.Piece := Chess.Piece.Piece(kind:=kind3,at:=chessPos3);
var color4 : Chess.Color := Chess.Color.Black;
var kind4 : Chess.Kind := Chess.Kind.Pawn(c:=color4);
var chessPos4 : Chess.ChessPos := Chess.Pos.Pos(row:=0,col:=0);
expect 0 <= chessPos4.row < 8 && 0 <= chessPos4.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece4 : Chess.Piece := Chess.Piece.Piece(kind:=kind4,at:=chessPos4);
var piece5 : seq<Chess.Piece> := [piece0, piece1, piece2, piece3, piece4];
var validBoard0 : Chess.ValidBoard := Chess.Board.Board(pieces:=piece5);
expect Chess.BoardIsValid(validBoard0), "If this check fails at runtime, the test does not meet the type constraints";
expect Chess.BoardPreset(validBoard0), "If this check fails at runtime, the test does not meet the preconditions";
Chess.Describe(validBoard0);
}
method {:test} Test14() {
var color0 : Chess.Color := Chess.Color.White;
var kind0 : Chess.Kind := Chess.Kind.King(c:=color0);
var chessPos0 : Chess.ChessPos := Chess.Pos.Pos(row:=4,col:=5);
expect 0 <= chessPos0.row < 8 && 0 <= chessPos0.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece0 : Chess.Piece := Chess.Piece.Piece(kind:=kind0,at:=chessPos0);
var color1 : Chess.Color := Chess.Color.Black;
var kind1 : Chess.Kind := Chess.Kind.Knight(c:=color1);
var chessPos1 : Chess.ChessPos := Chess.Pos.Pos(row:=3,col:=7);
expect 0 <= chessPos1.row < 8 && 0 <= chessPos1.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece1 : Chess.Piece := Chess.Piece.Piece(kind:=kind1,at:=chessPos1);
var color2 : Chess.Color := Chess.Color.Black;
var kind2 : Chess.Kind := Chess.Kind.Knight(c:=color2);
var chessPos2 : Chess.ChessPos := Chess.Pos.Pos(row:=3,col:=3);
expect 0 <= chessPos2.row < 8 && 0 <= chessPos2.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece2 : Chess.Piece := Chess.Piece.Piece(kind:=kind2,at:=chessPos2);
var color3 : Chess.Color := Chess.Color.Black;
var kind3 : Chess.Kind := Chess.Kind.Pawn(c:=color3);
var chessPos3 : Chess.ChessPos := Chess.Pos.Pos(row:=1,col:=2);
expect 0 <= chessPos3.row < 8 && 0 <= chessPos3.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece3 : Chess.Piece := Chess.Piece.Piece(kind:=kind3,at:=chessPos3);
var color4 : Chess.Color := Chess.Color.Black;
var kind4 : Chess.Kind := Chess.Kind.Pawn(c:=color4);
var chessPos4 : Chess.ChessPos := Chess.Pos.Pos(row:=0,col:=1);
expect 0 <= chessPos4.row < 8 && 0 <= chessPos4.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece4 : Chess.Piece := Chess.Piece.Piece(kind:=kind4,at:=chessPos4);
var piece5 : seq<Chess.Piece> := [piece0, piece1, piece2, piece3, piece4];
var validBoard0 : Chess.ValidBoard := Chess.Board.Board(pieces:=piece5);
expect Chess.BoardIsValid(validBoard0), "If this check fails at runtime, the test does not meet the type constraints";
expect Chess.BoardPreset(validBoard0), "If this check fails at runtime, the test does not meet the preconditions";
Chess.Describe(validBoard0);
}
method {:test} Test15() {
var color0 : Chess.Color := Chess.Color.White;
var kind0 : Chess.Kind := Chess.Kind.King(c:=color0);
var chessPos0 : Chess.ChessPos := Chess.Pos.Pos(row:=7,col:=5);
expect 0 <= chessPos0.row < 8 && 0 <= chessPos0.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece0 : Chess.Piece := Chess.Piece.Piece(kind:=kind0,at:=chessPos0);
var color1 : Chess.Color := Chess.Color.Black;
var kind1 : Chess.Kind := Chess.Kind.Knight(c:=color1);
var chessPos1 : Chess.ChessPos := Chess.Pos.Pos(row:=5,col:=4);
expect 0 <= chessPos1.row < 8 && 0 <= chessPos1.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece1 : Chess.Piece := Chess.Piece.Piece(kind:=kind1,at:=chessPos1);
var color2 : Chess.Color := Chess.Color.Black;
var kind2 : Chess.Kind := Chess.Kind.Knight(c:=color2);
var chessPos2 : Chess.ChessPos := Chess.Pos.Pos(row:=6,col:=3);
expect 0 <= chessPos2.row < 8 && 0 <= chessPos2.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece2 : Chess.Piece := Chess.Piece.Piece(kind:=kind2,at:=chessPos2);
var color3 : Chess.Color := Chess.Color.Black;
var kind3 : Chess.Kind := Chess.Kind.Pawn(c:=color3);
var chessPos3 : Chess.ChessPos := Chess.Pos.Pos(row:=4,col:=1);
expect 0 <= chessPos3.row < 8 && 0 <= chessPos3.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece3 : Chess.Piece := Chess.Piece.Piece(kind:=kind3,at:=chessPos3);
var color4 : Chess.Color := Chess.Color.Black;
var kind4 : Chess.Kind := Chess.Kind.Pawn(c:=color4);
var chessPos4 : Chess.ChessPos := Chess.Pos.Pos(row:=3,col:=0);
expect 0 <= chessPos4.row < 8 && 0 <= chessPos4.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece4 : Chess.Piece := Chess.Piece.Piece(kind:=kind4,at:=chessPos4);
var piece5 : seq<Chess.Piece> := [piece0, piece1, piece2, piece3, piece4];
var validBoard0 : Chess.ValidBoard := Chess.Board.Board(pieces:=piece5);
expect Chess.BoardIsValid(validBoard0), "If this check fails at runtime, the test does not meet the type constraints";
expect Chess.BoardPreset(validBoard0), "If this check fails at runtime, the test does not meet the preconditions";
Chess.Describe(validBoard0);
}
method {:test} Test20() {
var color0 : Chess.Color := Chess.Color.White;
var kind0 : Chess.Kind := Chess.Kind.King(c:=color0);
var chessPos0 : Chess.ChessPos := Chess.Pos.Pos(row:=5,col:=6);
expect 0 <= chessPos0.row < 8 && 0 <= chessPos0.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece0 : Chess.Piece := Chess.Piece.Piece(kind:=kind0,at:=chessPos0);
var color1 : Chess.Color := Chess.Color.Black;
var kind1 : Chess.Kind := Chess.Kind.Knight(c:=color1);
var chessPos1 : Chess.ChessPos := Chess.Pos.Pos(row:=4,col:=4);
expect 0 <= chessPos1.row < 8 && 0 <= chessPos1.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece1 : Chess.Piece := Chess.Piece.Piece(kind:=kind1,at:=chessPos1);
var color2 : Chess.Color := Chess.Color.Black;
var kind2 : Chess.Kind := Chess.Kind.Knight(c:=color2);
var chessPos2 : Chess.ChessPos := Chess.Pos.Pos(row:=3,col:=3);
expect 0 <= chessPos2.row < 8 && 0 <= chessPos2.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece2 : Chess.Piece := Chess.Piece.Piece(kind:=kind2,at:=chessPos2);
var color3 : Chess.Color := Chess.Color.Black;
var kind3 : Chess.Kind := Chess.Kind.Pawn(c:=color3);
var chessPos3 : Chess.ChessPos := Chess.Pos.Pos(row:=2,col:=2);
expect 0 <= chessPos3.row < 8 && 0 <= chessPos3.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece3 : Chess.Piece := Chess.Piece.Piece(kind:=kind3,at:=chessPos3);
var color4 : Chess.Color := Chess.Color.Black;
var kind4 : Chess.Kind := Chess.Kind.Pawn(c:=color4);
var chessPos4 : Chess.ChessPos := Chess.Pos.Pos(row:=1,col:=1);
expect 0 <= chessPos4.row < 8 && 0 <= chessPos4.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece4 : Chess.Piece := Chess.Piece.Piece(kind:=kind4,at:=chessPos4);
var piece5 : seq<Chess.Piece> := [piece0, piece1, piece2, piece3, piece4];
var validBoard0 : Chess.ValidBoard := Chess.Board.Board(pieces:=piece5);
expect Chess.BoardIsValid(validBoard0), "If this check fails at runtime, the test does not meet the type constraints";
expect Chess.BoardPreset(validBoard0), "If this check fails at runtime, the test does not meet the preconditions";
Chess.Describe(validBoard0);
}
method {:test} Test22() {
var color0 : Chess.Color := Chess.Color.White;
var kind0 : Chess.Kind := Chess.Kind.King(c:=color0);
var chessPos0 : Chess.ChessPos := Chess.Pos.Pos(row:=6,col:=5);
expect 0 <= chessPos0.row < 8 && 0 <= chessPos0.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece0 : Chess.Piece := Chess.Piece.Piece(kind:=kind0,at:=chessPos0);
var color1 : Chess.Color := Chess.Color.Black;
var kind1 : Chess.Kind := Chess.Kind.Knight(c:=color1);
var chessPos1 : Chess.ChessPos := Chess.Pos.Pos(row:=5,col:=3);
expect 0 <= chessPos1.row < 8 && 0 <= chessPos1.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece1 : Chess.Piece := Chess.Piece.Piece(kind:=kind1,at:=chessPos1);
var color2 : Chess.Color := Chess.Color.Black;
var kind2 : Chess.Kind := Chess.Kind.Knight(c:=color2);
var chessPos2 : Chess.ChessPos := Chess.Pos.Pos(row:=4,col:=2);
expect 0 <= chessPos2.row < 8 && 0 <= chessPos2.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece2 : Chess.Piece := Chess.Piece.Piece(kind:=kind2,at:=chessPos2);
var color3 : Chess.Color := Chess.Color.Black;
var kind3 : Chess.Kind := Chess.Kind.Pawn(c:=color3);
var chessPos3 : Chess.ChessPos := Chess.Pos.Pos(row:=3,col:=1);
expect 0 <= chessPos3.row < 8 && 0 <= chessPos3.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece3 : Chess.Piece := Chess.Piece.Piece(kind:=kind3,at:=chessPos3);
var color4 : Chess.Color := Chess.Color.Black;
var kind4 : Chess.Kind := Chess.Kind.Pawn(c:=color4);
var chessPos4 : Chess.ChessPos := Chess.Pos.Pos(row:=2,col:=0);
expect 0 <= chessPos4.row < 8 && 0 <= chessPos4.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece4 : Chess.Piece := Chess.Piece.Piece(kind:=kind4,at:=chessPos4);
var piece5 : seq<Chess.Piece> := [piece0, piece1, piece2, piece3, piece4];
var validBoard0 : Chess.ValidBoard := Chess.Board.Board(pieces:=piece5);
expect Chess.BoardIsValid(validBoard0), "If this check fails at runtime, the test does not meet the type constraints";
expect Chess.BoardPreset(validBoard0), "If this check fails at runtime, the test does not meet the preconditions";
Chess.Describe(validBoard0);
}
method {:test} Test23() {
var color0 : Chess.Color := Chess.Color.White;
var kind0 : Chess.Kind := Chess.Kind.King(c:=color0);
var chessPos0 : Chess.ChessPos := Chess.Pos.Pos(row:=6,col:=7);
expect 0 <= chessPos0.row < 8 && 0 <= chessPos0.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece0 : Chess.Piece := Chess.Piece.Piece(kind:=kind0,at:=chessPos0);
var color1 : Chess.Color := Chess.Color.Black;
var kind1 : Chess.Kind := Chess.Kind.Knight(c:=color1);
var chessPos1 : Chess.ChessPos := Chess.Pos.Pos(row:=4,col:=6);
expect 0 <= chessPos1.row < 8 && 0 <= chessPos1.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece1 : Chess.Piece := Chess.Piece.Piece(kind:=kind1,at:=chessPos1);
var color2 : Chess.Color := Chess.Color.Black;
var kind2 : Chess.Kind := Chess.Kind.Knight(c:=color2);
var chessPos2 : Chess.ChessPos := Chess.Pos.Pos(row:=5,col:=5);
expect 0 <= chessPos2.row < 8 && 0 <= chessPos2.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece2 : Chess.Piece := Chess.Piece.Piece(kind:=kind2,at:=chessPos2);
var color3 : Chess.Color := Chess.Color.Black;
var kind3 : Chess.Kind := Chess.Kind.Pawn(c:=color3);
var chessPos3 : Chess.ChessPos := Chess.Pos.Pos(row:=3,col:=1);
expect 0 <= chessPos3.row < 8 && 0 <= chessPos3.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece3 : Chess.Piece := Chess.Piece.Piece(kind:=kind3,at:=chessPos3);
var color4 : Chess.Color := Chess.Color.Black;
var kind4 : Chess.Kind := Chess.Kind.Pawn(c:=color4);
var chessPos4 : Chess.ChessPos := Chess.Pos.Pos(row:=2,col:=0);
expect 0 <= chessPos4.row < 8 && 0 <= chessPos4.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece4 : Chess.Piece := Chess.Piece.Piece(kind:=kind4,at:=chessPos4);
var piece5 : seq<Chess.Piece> := [piece0, piece1, piece2, piece3, piece4];
var validBoard0 : Chess.ValidBoard := Chess.Board.Board(pieces:=piece5);
expect Chess.BoardIsValid(validBoard0), "If this check fails at runtime, the test does not meet the type constraints";
expect Chess.BoardPreset(validBoard0), "If this check fails at runtime, the test does not meet the preconditions";
Chess.Describe(validBoard0);
}
method {:test} Test24() {
var color0 : Chess.Color := Chess.Color.White;
var kind0 : Chess.Kind := Chess.Kind.King(c:=color0);
var chessPos0 : Chess.ChessPos := Chess.Pos.Pos(row:=0,col:=7);
expect 0 <= chessPos0.row < 8 && 0 <= chessPos0.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece0 : Chess.Piece := Chess.Piece.Piece(kind:=kind0,at:=chessPos0);
var color1 : Chess.Color := Chess.Color.Black;
var kind1 : Chess.Kind := Chess.Kind.Knight(c:=color1);
var chessPos1 : Chess.ChessPos := Chess.Pos.Pos(row:=1,col:=5);
expect 0 <= chessPos1.row < 8 && 0 <= chessPos1.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece1 : Chess.Piece := Chess.Piece.Piece(kind:=kind1,at:=chessPos1);
var color2 : Chess.Color := Chess.Color.Black;
var kind2 : Chess.Kind := Chess.Kind.Knight(c:=color2);
var chessPos2 : Chess.ChessPos := Chess.Pos.Pos(row:=1,col:=4);
expect 0 <= chessPos2.row < 8 && 0 <= chessPos2.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece2 : Chess.Piece := Chess.Piece.Piece(kind:=kind2,at:=chessPos2);
var color3 : Chess.Color := Chess.Color.Black;
var kind3 : Chess.Kind := Chess.Kind.Pawn(c:=color3);
var chessPos3 : Chess.ChessPos := Chess.Pos.Pos(row:=0,col:=6);
expect 0 <= chessPos3.row < 8 && 0 <= chessPos3.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece3 : Chess.Piece := Chess.Piece.Piece(kind:=kind3,at:=chessPos3);
var color4 : Chess.Color := Chess.Color.Black;
var kind4 : Chess.Kind := Chess.Kind.Pawn(c:=color4);
var chessPos4 : Chess.ChessPos := Chess.Pos.Pos(row:=0,col:=5);
expect 0 <= chessPos4.row < 8 && 0 <= chessPos4.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece4 : Chess.Piece := Chess.Piece.Piece(kind:=kind4,at:=chessPos4);
var piece5 : seq<Chess.Piece> := [piece0, piece1, piece2, piece3, piece4];
var validBoard0 : Chess.ValidBoard := Chess.Board.Board(pieces:=piece5);
expect Chess.BoardIsValid(validBoard0), "If this check fails at runtime, the test does not meet the type constraints";
expect Chess.BoardPreset(validBoard0), "If this check fails at runtime, the test does not meet the preconditions";
Chess.Describe(validBoard0);
}
method {:test} Test25() {
var color0 : Chess.Color := Chess.Color.White;
var kind0 : Chess.Kind := Chess.Kind.King(c:=color0);
var chessPos0 : Chess.ChessPos := Chess.Pos.Pos(row:=3,col:=6);
expect 0 <= chessPos0.row < 8 && 0 <= chessPos0.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece0 : Chess.Piece := Chess.Piece.Piece(kind:=kind0,at:=chessPos0);
var color1 : Chess.Color := Chess.Color.Black;
var kind1 : Chess.Kind := Chess.Kind.Knight(c:=color1);
var chessPos1 : Chess.ChessPos := Chess.Pos.Pos(row:=2,col:=4);
expect 0 <= chessPos1.row < 8 && 0 <= chessPos1.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece1 : Chess.Piece := Chess.Piece.Piece(kind:=kind1,at:=chessPos1);
var color2 : Chess.Color := Chess.Color.Black;
var kind2 : Chess.Kind := Chess.Kind.Knight(c:=color2);
var chessPos2 : Chess.ChessPos := Chess.Pos.Pos(row:=1,col:=2);
expect 0 <= chessPos2.row < 8 && 0 <= chessPos2.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece2 : Chess.Piece := Chess.Piece.Piece(kind:=kind2,at:=chessPos2);
var color3 : Chess.Color := Chess.Color.Black;
var kind3 : Chess.Kind := Chess.Kind.Pawn(c:=color3);
var chessPos3 : Chess.ChessPos := Chess.Pos.Pos(row:=6,col:=1);
expect 0 <= chessPos3.row < 8 && 0 <= chessPos3.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece3 : Chess.Piece := Chess.Piece.Piece(kind:=kind3,at:=chessPos3);
var color4 : Chess.Color := Chess.Color.Black;
var kind4 : Chess.Kind := Chess.Kind.Pawn(c:=color4);
var chessPos4 : Chess.ChessPos := Chess.Pos.Pos(row:=5,col:=0);
expect 0 <= chessPos4.row < 8 && 0 <= chessPos4.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece4 : Chess.Piece := Chess.Piece.Piece(kind:=kind4,at:=chessPos4);
var piece5 : seq<Chess.Piece> := [piece0, piece1, piece2, piece3, piece4];
var validBoard0 : Chess.ValidBoard := Chess.Board.Board(pieces:=piece5);
expect Chess.BoardIsValid(validBoard0), "If this check fails at runtime, the test does not meet the type constraints";
expect Chess.BoardPreset(validBoard0), "If this check fails at runtime, the test does not meet the preconditions";
Chess.Describe(validBoard0);
}
method {:test} Test26() {
var color0 : Chess.Color := Chess.Color.White;
var kind0 : Chess.Kind := Chess.Kind.King(c:=color0);
var chessPos0 : Chess.ChessPos := Chess.Pos.Pos(row:=7,col:=6);
expect 0 <= chessPos0.row < 8 && 0 <= chessPos0.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece0 : Chess.Piece := Chess.Piece.Piece(kind:=kind0,at:=chessPos0);
var color1 : Chess.Color := Chess.Color.Black;
var kind1 : Chess.Kind := Chess.Kind.Knight(c:=color1);
var chessPos1 : Chess.ChessPos := Chess.Pos.Pos(row:=3,col:=3);
expect 0 <= chessPos1.row < 8 && 0 <= chessPos1.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece1 : Chess.Piece := Chess.Piece.Piece(kind:=kind1,at:=chessPos1);
var color2 : Chess.Color := Chess.Color.Black;
var kind2 : Chess.Kind := Chess.Kind.Knight(c:=color2);
var chessPos2 : Chess.ChessPos := Chess.Pos.Pos(row:=2,col:=2);
expect 0 <= chessPos2.row < 8 && 0 <= chessPos2.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece2 : Chess.Piece := Chess.Piece.Piece(kind:=kind2,at:=chessPos2);
var color3 : Chess.Color := Chess.Color.Black;
var kind3 : Chess.Kind := Chess.Kind.Pawn(c:=color3);
var chessPos3 : Chess.ChessPos := Chess.Pos.Pos(row:=1,col:=1);
expect 0 <= chessPos3.row < 8 && 0 <= chessPos3.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece3 : Chess.Piece := Chess.Piece.Piece(kind:=kind3,at:=chessPos3);
var color4 : Chess.Color := Chess.Color.Black;
var kind4 : Chess.Kind := Chess.Kind.Pawn(c:=color4);
var chessPos4 : Chess.ChessPos := Chess.Pos.Pos(row:=0,col:=0);
expect 0 <= chessPos4.row < 8 && 0 <= chessPos4.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece4 : Chess.Piece := Chess.Piece.Piece(kind:=kind4,at:=chessPos4);
var piece5 : seq<Chess.Piece> := [piece0, piece1, piece2, piece3, piece4];
var validBoard0 : Chess.ValidBoard := Chess.Board.Board(pieces:=piece5);
expect Chess.BoardIsValid(validBoard0), "If this check fails at runtime, the test does not meet the type constraints";
expect Chess.BoardPreset(validBoard0), "If this check fails at runtime, the test does not meet the preconditions";
Chess.Describe(validBoard0);
}
method {:test} Test27() {
var color0 : Chess.Color := Chess.Color.White;
var kind0 : Chess.Kind := Chess.Kind.King(c:=color0);
var chessPos0 : Chess.ChessPos := Chess.Pos.Pos(row:=7,col:=0);
expect 0 <= chessPos0.row < 8 && 0 <= chessPos0.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece0 : Chess.Piece := Chess.Piece.Piece(kind:=kind0,at:=chessPos0);
var color1 : Chess.Color := Chess.Color.Black;
var kind1 : Chess.Kind := Chess.Kind.Knight(c:=color1);
var chessPos1 : Chess.ChessPos := Chess.Pos.Pos(row:=4,col:=0);
expect 0 <= chessPos1.row < 8 && 0 <= chessPos1.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece1 : Chess.Piece := Chess.Piece.Piece(kind:=kind1,at:=chessPos1);
var color2 : Chess.Color := Chess.Color.Black;
var kind2 : Chess.Kind := Chess.Kind.Knight(c:=color2);
var chessPos2 : Chess.ChessPos := Chess.Pos.Pos(row:=5,col:=2);
expect 0 <= chessPos2.row < 8 && 0 <= chessPos2.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece2 : Chess.Piece := Chess.Piece.Piece(kind:=kind2,at:=chessPos2);
var color3 : Chess.Color := Chess.Color.Black;
var kind3 : Chess.Kind := Chess.Kind.Pawn(c:=color3);
var chessPos3 : Chess.ChessPos := Chess.Pos.Pos(row:=6,col:=1);
expect 0 <= chessPos3.row < 8 && 0 <= chessPos3.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece3 : Chess.Piece := Chess.Piece.Piece(kind:=kind3,at:=chessPos3);
var color4 : Chess.Color := Chess.Color.Black;
var kind4 : Chess.Kind := Chess.Kind.Pawn(c:=color4);
var chessPos4 : Chess.ChessPos := Chess.Pos.Pos(row:=3,col:=0);
expect 0 <= chessPos4.row < 8 && 0 <= chessPos4.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece4 : Chess.Piece := Chess.Piece.Piece(kind:=kind4,at:=chessPos4);
var piece5 : seq<Chess.Piece> := [piece0, piece1, piece2, piece3, piece4];
var validBoard0 : Chess.ValidBoard := Chess.Board.Board(pieces:=piece5);
expect Chess.BoardIsValid(validBoard0), "If this check fails at runtime, the test does not meet the type constraints";
expect Chess.BoardPreset(validBoard0), "If this check fails at runtime, the test does not meet the preconditions";
Chess.Describe(validBoard0);
}
method {:test} Test28() {
var color0 : Chess.Color := Chess.Color.White;
var kind0 : Chess.Kind := Chess.Kind.King(c:=color0);
var chessPos0 : Chess.ChessPos := Chess.Pos.Pos(row:=6,col:=4);
expect 0 <= chessPos0.row < 8 && 0 <= chessPos0.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece0 : Chess.Piece := Chess.Piece.Piece(kind:=kind0,at:=chessPos0);
var color1 : Chess.Color := Chess.Color.Black;
var kind1 : Chess.Kind := Chess.Kind.Knight(c:=color1);
var chessPos1 : Chess.ChessPos := Chess.Pos.Pos(row:=3,col:=1);
expect 0 <= chessPos1.row < 8 && 0 <= chessPos1.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece1 : Chess.Piece := Chess.Piece.Piece(kind:=kind1,at:=chessPos1);
var color2 : Chess.Color := Chess.Color.Black;
var kind2 : Chess.Kind := Chess.Kind.Knight(c:=color2);
var chessPos2 : Chess.ChessPos := Chess.Pos.Pos(row:=0,col:=0);
expect 0 <= chessPos2.row < 8 && 0 <= chessPos2.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece2 : Chess.Piece := Chess.Piece.Piece(kind:=kind2,at:=chessPos2);
var color3 : Chess.Color := Chess.Color.Black;
var kind3 : Chess.Kind := Chess.Kind.Pawn(c:=color3);
var chessPos3 : Chess.ChessPos := Chess.Pos.Pos(row:=5,col:=5);
expect 0 <= chessPos3.row < 8 && 0 <= chessPos3.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece3 : Chess.Piece := Chess.Piece.Piece(kind:=kind3,at:=chessPos3);
var color4 : Chess.Color := Chess.Color.Black;
var kind4 : Chess.Kind := Chess.Kind.Pawn(c:=color4);
var chessPos4 : Chess.ChessPos := Chess.Pos.Pos(row:=5,col:=3);
expect 0 <= chessPos4.row < 8 && 0 <= chessPos4.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece4 : Chess.Piece := Chess.Piece.Piece(kind:=kind4,at:=chessPos4);
var piece5 : seq<Chess.Piece> := [piece0, piece1, piece2, piece3, piece4];
var validBoard0 : Chess.ValidBoard := Chess.Board.Board(pieces:=piece5);
expect Chess.BoardIsValid(validBoard0), "If this check fails at runtime, the test does not meet the type constraints";
expect Chess.BoardPreset(validBoard0), "If this check fails at runtime, the test does not meet the preconditions";
Chess.Describe(validBoard0);
}
method {:test} Test29() {
var color0 : Chess.Color := Chess.Color.White;
var kind0 : Chess.Kind := Chess.Kind.King(c:=color0);
var chessPos0 : Chess.ChessPos := Chess.Pos.Pos(row:=7,col:=0);
expect 0 <= chessPos0.row < 8 && 0 <= chessPos0.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece0 : Chess.Piece := Chess.Piece.Piece(kind:=kind0,at:=chessPos0);
var color1 : Chess.Color := Chess.Color.Black;
var kind1 : Chess.Kind := Chess.Kind.Knight(c:=color1);
var chessPos1 : Chess.ChessPos := Chess.Pos.Pos(row:=6,col:=0);
expect 0 <= chessPos1.row < 8 && 0 <= chessPos1.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece1 : Chess.Piece := Chess.Piece.Piece(kind:=kind1,at:=chessPos1);
var color2 : Chess.Color := Chess.Color.Black;
var kind2 : Chess.Kind := Chess.Kind.Knight(c:=color2);
var chessPos2 : Chess.ChessPos := Chess.Pos.Pos(row:=5,col:=2);
expect 0 <= chessPos2.row < 8 && 0 <= chessPos2.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece2 : Chess.Piece := Chess.Piece.Piece(kind:=kind2,at:=chessPos2);
var color3 : Chess.Color := Chess.Color.Black;
var kind3 : Chess.Kind := Chess.Kind.Pawn(c:=color3);
var chessPos3 : Chess.ChessPos := Chess.Pos.Pos(row:=6,col:=1);
expect 0 <= chessPos3.row < 8 && 0 <= chessPos3.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece3 : Chess.Piece := Chess.Piece.Piece(kind:=kind3,at:=chessPos3);
var color4 : Chess.Color := Chess.Color.Black;
var kind4 : Chess.Kind := Chess.Kind.Pawn(c:=color4);
var chessPos4 : Chess.ChessPos := Chess.Pos.Pos(row:=5,col:=0);
expect 0 <= chessPos4.row < 8 && 0 <= chessPos4.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece4 : Chess.Piece := Chess.Piece.Piece(kind:=kind4,at:=chessPos4);
var piece5 : seq<Chess.Piece> := [piece0, piece1, piece2, piece3, piece4];
var validBoard0 : Chess.ValidBoard := Chess.Board.Board(pieces:=piece5);
expect Chess.BoardIsValid(validBoard0), "If this check fails at runtime, the test does not meet the type constraints";
expect Chess.BoardPreset(validBoard0), "If this check fails at runtime, the test does not meet the preconditions";
Chess.Describe(validBoard0);
}
method {:test} Test30() {
var color0 : Chess.Color := Chess.Color.White;
var kind0 : Chess.Kind := Chess.Kind.King(c:=color0);
var chessPos0 : Chess.ChessPos := Chess.Pos.Pos(row:=7,col:=5);
expect 0 <= chessPos0.row < 8 && 0 <= chessPos0.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece0 : Chess.Piece := Chess.Piece.Piece(kind:=kind0,at:=chessPos0);
var color1 : Chess.Color := Chess.Color.Black;
var kind1 : Chess.Kind := Chess.Kind.Knight(c:=color1);
var chessPos1 : Chess.ChessPos := Chess.Pos.Pos(row:=3,col:=2);
expect 0 <= chessPos1.row < 8 && 0 <= chessPos1.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece1 : Chess.Piece := Chess.Piece.Piece(kind:=kind1,at:=chessPos1);
var color2 : Chess.Color := Chess.Color.Black;
var kind2 : Chess.Kind := Chess.Kind.Knight(c:=color2);
var chessPos2 : Chess.ChessPos := Chess.Pos.Pos(row:=2,col:=1);
expect 0 <= chessPos2.row < 8 && 0 <= chessPos2.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece2 : Chess.Piece := Chess.Piece.Piece(kind:=kind2,at:=chessPos2);
var color3 : Chess.Color := Chess.Color.Black;
var kind3 : Chess.Kind := Chess.Kind.Pawn(c:=color3);
var chessPos3 : Chess.ChessPos := Chess.Pos.Pos(row:=6,col:=4);
expect 0 <= chessPos3.row < 8 && 0 <= chessPos3.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece3 : Chess.Piece := Chess.Piece.Piece(kind:=kind3,at:=chessPos3);
var color4 : Chess.Color := Chess.Color.Black;
var kind4 : Chess.Kind := Chess.Kind.Pawn(c:=color4);
var chessPos4 : Chess.ChessPos := Chess.Pos.Pos(row:=1,col:=0);
expect 0 <= chessPos4.row < 8 && 0 <= chessPos4.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece4 : Chess.Piece := Chess.Piece.Piece(kind:=kind4,at:=chessPos4);
var piece5 : seq<Chess.Piece> := [piece0, piece1, piece2, piece3, piece4];
var validBoard0 : Chess.ValidBoard := Chess.Board.Board(pieces:=piece5);
expect Chess.BoardIsValid(validBoard0), "If this check fails at runtime, the test does not meet the type constraints";
expect Chess.BoardPreset(validBoard0), "If this check fails at runtime, the test does not meet the preconditions";
Chess.Describe(validBoard0);
}
method {:test} Test31() {
var color0 : Chess.Color := Chess.Color.White;
var kind0 : Chess.Kind := Chess.Kind.King(c:=color0);
var chessPos0 : Chess.ChessPos := Chess.Pos.Pos(row:=6,col:=7);
expect 0 <= chessPos0.row < 8 && 0 <= chessPos0.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece0 : Chess.Piece := Chess.Piece.Piece(kind:=kind0,at:=chessPos0);
var color1 : Chess.Color := Chess.Color.Black;
var kind1 : Chess.Kind := Chess.Kind.Knight(c:=color1);
var chessPos1 : Chess.ChessPos := Chess.Pos.Pos(row:=4,col:=4);
expect 0 <= chessPos1.row < 8 && 0 <= chessPos1.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece1 : Chess.Piece := Chess.Piece.Piece(kind:=kind1,at:=chessPos1);
var color2 : Chess.Color := Chess.Color.Black;
var kind2 : Chess.Kind := Chess.Kind.Knight(c:=color2);
var chessPos2 : Chess.ChessPos := Chess.Pos.Pos(row:=1,col:=3);
expect 0 <= chessPos2.row < 8 && 0 <= chessPos2.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece2 : Chess.Piece := Chess.Piece.Piece(kind:=kind2,at:=chessPos2);
var color3 : Chess.Color := Chess.Color.Black;
var kind3 : Chess.Kind := Chess.Kind.Pawn(c:=color3);
var chessPos3 : Chess.ChessPos := Chess.Pos.Pos(row:=5,col:=6);
expect 0 <= chessPos3.row < 8 && 0 <= chessPos3.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece3 : Chess.Piece := Chess.Piece.Piece(kind:=kind3,at:=chessPos3);
var color4 : Chess.Color := Chess.Color.Black;
var kind4 : Chess.Kind := Chess.Kind.Pawn(c:=color4);
var chessPos4 : Chess.ChessPos := Chess.Pos.Pos(row:=0,col:=2);
expect 0 <= chessPos4.row < 8 && 0 <= chessPos4.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece4 : Chess.Piece := Chess.Piece.Piece(kind:=kind4,at:=chessPos4);
var piece5 : seq<Chess.Piece> := [piece0, piece1, piece2, piece3, piece4];
var validBoard0 : Chess.ValidBoard := Chess.Board.Board(pieces:=piece5);
expect Chess.BoardIsValid(validBoard0), "If this check fails at runtime, the test does not meet the type constraints";
expect Chess.BoardPreset(validBoard0), "If this check fails at runtime, the test does not meet the preconditions";
Chess.Describe(validBoard0);
}
method {:test} Test32() {
var color0 : Chess.Color := Chess.Color.White;
var kind0 : Chess.Kind := Chess.Kind.King(c:=color0);
var chessPos0 : Chess.ChessPos := Chess.Pos.Pos(row:=5,col:=7);
expect 0 <= chessPos0.row < 8 && 0 <= chessPos0.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece0 : Chess.Piece := Chess.Piece.Piece(kind:=kind0,at:=chessPos0);
var color1 : Chess.Color := Chess.Color.Black;
var kind1 : Chess.Kind := Chess.Kind.Knight(c:=color1);
var chessPos1 : Chess.ChessPos := Chess.Pos.Pos(row:=3,col:=3);
expect 0 <= chessPos1.row < 8 && 0 <= chessPos1.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece1 : Chess.Piece := Chess.Piece.Piece(kind:=kind1,at:=chessPos1);
var color2 : Chess.Color := Chess.Color.Black;
var kind2 : Chess.Kind := Chess.Kind.Knight(c:=color2);
var chessPos2 : Chess.ChessPos := Chess.Pos.Pos(row:=1,col:=2);
expect 0 <= chessPos2.row < 8 && 0 <= chessPos2.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece2 : Chess.Piece := Chess.Piece.Piece(kind:=kind2,at:=chessPos2);
var color3 : Chess.Color := Chess.Color.Black;
var kind3 : Chess.Kind := Chess.Kind.Pawn(c:=color3);
var chessPos3 : Chess.ChessPos := Chess.Pos.Pos(row:=4,col:=6);
expect 0 <= chessPos3.row < 8 && 0 <= chessPos3.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece3 : Chess.Piece := Chess.Piece.Piece(kind:=kind3,at:=chessPos3);
var color4 : Chess.Color := Chess.Color.Black;
var kind4 : Chess.Kind := Chess.Kind.Pawn(c:=color4);
var chessPos4 : Chess.ChessPos := Chess.Pos.Pos(row:=0,col:=1);
expect 0 <= chessPos4.row < 8 && 0 <= chessPos4.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece4 : Chess.Piece := Chess.Piece.Piece(kind:=kind4,at:=chessPos4);
var piece5 : seq<Chess.Piece> := [piece0, piece1, piece2, piece3, piece4];
var validBoard0 : Chess.ValidBoard := Chess.Board.Board(pieces:=piece5);
expect Chess.BoardIsValid(validBoard0), "If this check fails at runtime, the test does not meet the type constraints";
expect Chess.BoardPreset(validBoard0), "If this check fails at runtime, the test does not meet the preconditions";
Chess.Describe(validBoard0);
}
method {:test} Test33() {
var color0 : Chess.Color := Chess.Color.White;
var kind0 : Chess.Kind := Chess.Kind.King(c:=color0);
var chessPos0 : Chess.ChessPos := Chess.Pos.Pos(row:=5,col:=7);
expect 0 <= chessPos0.row < 8 && 0 <= chessPos0.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece0 : Chess.Piece := Chess.Piece.Piece(kind:=kind0,at:=chessPos0);
var color1 : Chess.Color := Chess.Color.Black;
var kind1 : Chess.Kind := Chess.Kind.Knight(c:=color1);
var chessPos1 : Chess.ChessPos := Chess.Pos.Pos(row:=2,col:=3);
expect 0 <= chessPos1.row < 8 && 0 <= chessPos1.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece1 : Chess.Piece := Chess.Piece.Piece(kind:=kind1,at:=chessPos1);
var color2 : Chess.Color := Chess.Color.Black;
var kind2 : Chess.Kind := Chess.Kind.Knight(c:=color2);
var chessPos2 : Chess.ChessPos := Chess.Pos.Pos(row:=1,col:=2);
expect 0 <= chessPos2.row < 8 && 0 <= chessPos2.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece2 : Chess.Piece := Chess.Piece.Piece(kind:=kind2,at:=chessPos2);
var color3 : Chess.Color := Chess.Color.Black;
var kind3 : Chess.Kind := Chess.Kind.Pawn(c:=color3);
var chessPos3 : Chess.ChessPos := Chess.Pos.Pos(row:=4,col:=6);
expect 0 <= chessPos3.row < 8 && 0 <= chessPos3.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece3 : Chess.Piece := Chess.Piece.Piece(kind:=kind3,at:=chessPos3);
var color4 : Chess.Color := Chess.Color.Black;
var kind4 : Chess.Kind := Chess.Kind.Pawn(c:=color4);
var chessPos4 : Chess.ChessPos := Chess.Pos.Pos(row:=0,col:=4);
expect 0 <= chessPos4.row < 8 && 0 <= chessPos4.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece4 : Chess.Piece := Chess.Piece.Piece(kind:=kind4,at:=chessPos4);
var piece5 : seq<Chess.Piece> := [piece0, piece1, piece2, piece3, piece4];
var validBoard0 : Chess.ValidBoard := Chess.Board.Board(pieces:=piece5);
expect Chess.BoardIsValid(validBoard0), "If this check fails at runtime, the test does not meet the type constraints";
expect Chess.BoardPreset(validBoard0), "If this check fails at runtime, the test does not meet the preconditions";
Chess.Describe(validBoard0);
}
method {:test} Test34() {
var color0 : Chess.Color := Chess.Color.White;
var kind0 : Chess.Kind := Chess.Kind.King(c:=color0);
var chessPos0 : Chess.ChessPos := Chess.Pos.Pos(row:=7,col:=4);
expect 0 <= chessPos0.row < 8 && 0 <= chessPos0.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece0 : Chess.Piece := Chess.Piece.Piece(kind:=kind0,at:=chessPos0);
var color1 : Chess.Color := Chess.Color.Black;
var kind1 : Chess.Kind := Chess.Kind.Knight(c:=color1);
var chessPos1 : Chess.ChessPos := Chess.Pos.Pos(row:=2,col:=1);
expect 0 <= chessPos1.row < 8 && 0 <= chessPos1.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece1 : Chess.Piece := Chess.Piece.Piece(kind:=kind1,at:=chessPos1);
var color2 : Chess.Color := Chess.Color.Black;
var kind2 : Chess.Kind := Chess.Kind.Knight(c:=color2);
var chessPos2 : Chess.ChessPos := Chess.Pos.Pos(row:=1,col:=0);
expect 0 <= chessPos2.row < 8 && 0 <= chessPos2.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece2 : Chess.Piece := Chess.Piece.Piece(kind:=kind2,at:=chessPos2);
var color3 : Chess.Color := Chess.Color.Black;
var kind3 : Chess.Kind := Chess.Kind.Pawn(c:=color3);
var chessPos3 : Chess.ChessPos := Chess.Pos.Pos(row:=6,col:=5);
expect 0 <= chessPos3.row < 8 && 0 <= chessPos3.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece3 : Chess.Piece := Chess.Piece.Piece(kind:=kind3,at:=chessPos3);
var color4 : Chess.Color := Chess.Color.Black;
var kind4 : Chess.Kind := Chess.Kind.Pawn(c:=color4);
var chessPos4 : Chess.ChessPos := Chess.Pos.Pos(row:=6,col:=3);
expect 0 <= chessPos4.row < 8 && 0 <= chessPos4.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece4 : Chess.Piece := Chess.Piece.Piece(kind:=kind4,at:=chessPos4);
var piece5 : seq<Chess.Piece> := [piece0, piece1, piece2, piece3, piece4];
var validBoard0 : Chess.ValidBoard := Chess.Board.Board(pieces:=piece5);
expect Chess.BoardIsValid(validBoard0), "If this check fails at runtime, the test does not meet the type constraints";
expect Chess.BoardPreset(validBoard0), "If this check fails at runtime, the test does not meet the preconditions";
Chess.Describe(validBoard0);
}
method {:test} Test36() {
var color0 : Chess.Color := Chess.Color.White;
var kind0 : Chess.Kind := Chess.Kind.King(c:=color0);
var chessPos0 : Chess.ChessPos := Chess.Pos.Pos(row:=6,col:=7);
expect 0 <= chessPos0.row < 8 && 0 <= chessPos0.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece0 : Chess.Piece := Chess.Piece.Piece(kind:=kind0,at:=chessPos0);
var color1 : Chess.Color := Chess.Color.Black;
var kind1 : Chess.Kind := Chess.Kind.Knight(c:=color1);
var chessPos1 : Chess.ChessPos := Chess.Pos.Pos(row:=3,col:=4);
expect 0 <= chessPos1.row < 8 && 0 <= chessPos1.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece1 : Chess.Piece := Chess.Piece.Piece(kind:=kind1,at:=chessPos1);
var color2 : Chess.Color := Chess.Color.Black;
var kind2 : Chess.Kind := Chess.Kind.Knight(c:=color2);
var chessPos2 : Chess.ChessPos := Chess.Pos.Pos(row:=1,col:=2);
expect 0 <= chessPos2.row < 8 && 0 <= chessPos2.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece2 : Chess.Piece := Chess.Piece.Piece(kind:=kind2,at:=chessPos2);
var color3 : Chess.Color := Chess.Color.Black;
var kind3 : Chess.Kind := Chess.Kind.Pawn(c:=color3);
var chessPos3 : Chess.ChessPos := Chess.Pos.Pos(row:=5,col:=6);
expect 0 <= chessPos3.row < 8 && 0 <= chessPos3.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece3 : Chess.Piece := Chess.Piece.Piece(kind:=kind3,at:=chessPos3);
var color4 : Chess.Color := Chess.Color.Black;
var kind4 : Chess.Kind := Chess.Kind.Pawn(c:=color4);
var chessPos4 : Chess.ChessPos := Chess.Pos.Pos(row:=0,col:=1);
expect 0 <= chessPos4.row < 8 && 0 <= chessPos4.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece4 : Chess.Piece := Chess.Piece.Piece(kind:=kind4,at:=chessPos4);
var piece5 : seq<Chess.Piece> := [piece0, piece1, piece2, piece3, piece4];
var validBoard0 : Chess.ValidBoard := Chess.Board.Board(pieces:=piece5);
expect Chess.BoardIsValid(validBoard0), "If this check fails at runtime, the test does not meet the type constraints";
expect Chess.BoardPreset(validBoard0), "If this check fails at runtime, the test does not meet the preconditions";
Chess.Describe(validBoard0);
}
method {:test} Test37() {
var color0 : Chess.Color := Chess.Color.White;
var kind0 : Chess.Kind := Chess.Kind.King(c:=color0);
var chessPos0 : Chess.ChessPos := Chess.Pos.Pos(row:=7,col:=0);
expect 0 <= chessPos0.row < 8 && 0 <= chessPos0.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece0 : Chess.Piece := Chess.Piece.Piece(kind:=kind0,at:=chessPos0);
var color1 : Chess.Color := Chess.Color.Black;
var kind1 : Chess.Kind := Chess.Kind.Knight(c:=color1);
var chessPos1 : Chess.ChessPos := Chess.Pos.Pos(row:=5,col:=2);
expect 0 <= chessPos1.row < 8 && 0 <= chessPos1.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece1 : Chess.Piece := Chess.Piece.Piece(kind:=kind1,at:=chessPos1);
var color2 : Chess.Color := Chess.Color.Black;
var kind2 : Chess.Kind := Chess.Kind.Knight(c:=color2);
var chessPos2 : Chess.ChessPos := Chess.Pos.Pos(row:=4,col:=1);
expect 0 <= chessPos2.row < 8 && 0 <= chessPos2.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece2 : Chess.Piece := Chess.Piece.Piece(kind:=kind2,at:=chessPos2);
var color3 : Chess.Color := Chess.Color.Black;
var kind3 : Chess.Kind := Chess.Kind.Pawn(c:=color3);
var chessPos3 : Chess.ChessPos := Chess.Pos.Pos(row:=6,col:=1);
expect 0 <= chessPos3.row < 8 && 0 <= chessPos3.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece3 : Chess.Piece := Chess.Piece.Piece(kind:=kind3,at:=chessPos3);
var color4 : Chess.Color := Chess.Color.Black;
var kind4 : Chess.Kind := Chess.Kind.Pawn(c:=color4);
var chessPos4 : Chess.ChessPos := Chess.Pos.Pos(row:=5,col:=0);
expect 0 <= chessPos4.row < 8 && 0 <= chessPos4.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece4 : Chess.Piece := Chess.Piece.Piece(kind:=kind4,at:=chessPos4);
var piece5 : seq<Chess.Piece> := [piece0, piece1, piece2, piece3, piece4];
var validBoard0 : Chess.ValidBoard := Chess.Board.Board(pieces:=piece5);
expect Chess.BoardIsValid(validBoard0), "If this check fails at runtime, the test does not meet the type constraints";
expect Chess.BoardPreset(validBoard0), "If this check fails at runtime, the test does not meet the preconditions";
Chess.Describe(validBoard0);
}
method {:test} Test39() {
var color0 : Chess.Color := Chess.Color.White;
var kind0 : Chess.Kind := Chess.Kind.King(c:=color0);
var chessPos0 : Chess.ChessPos := Chess.Pos.Pos(row:=7,col:=7);
expect 0 <= chessPos0.row < 8 && 0 <= chessPos0.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece0 : Chess.Piece := Chess.Piece.Piece(kind:=kind0,at:=chessPos0);
var color1 : Chess.Color := Chess.Color.Black;
var kind1 : Chess.Kind := Chess.Kind.Knight(c:=color1);
var chessPos1 : Chess.ChessPos := Chess.Pos.Pos(row:=5,col:=5);
expect 0 <= chessPos1.row < 8 && 0 <= chessPos1.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece1 : Chess.Piece := Chess.Piece.Piece(kind:=kind1,at:=chessPos1);
var color2 : Chess.Color := Chess.Color.Black;
var kind2 : Chess.Kind := Chess.Kind.Knight(c:=color2);
var chessPos2 : Chess.ChessPos := Chess.Pos.Pos(row:=5,col:=4);
expect 0 <= chessPos2.row < 8 && 0 <= chessPos2.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece2 : Chess.Piece := Chess.Piece.Piece(kind:=kind2,at:=chessPos2);
var color3 : Chess.Color := Chess.Color.Black;
var kind3 : Chess.Kind := Chess.Kind.Pawn(c:=color3);
var chessPos3 : Chess.ChessPos := Chess.Pos.Pos(row:=3,col:=1);
expect 0 <= chessPos3.row < 8 && 0 <= chessPos3.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece3 : Chess.Piece := Chess.Piece.Piece(kind:=kind3,at:=chessPos3);
var color4 : Chess.Color := Chess.Color.Black;
var kind4 : Chess.Kind := Chess.Kind.Pawn(c:=color4);
var chessPos4 : Chess.ChessPos := Chess.Pos.Pos(row:=6,col:=6);
expect 0 <= chessPos4.row < 8 && 0 <= chessPos4.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece4 : Chess.Piece := Chess.Piece.Piece(kind:=kind4,at:=chessPos4);
var piece5 : seq<Chess.Piece> := [piece0, piece1, piece2, piece3, piece4];
var validBoard0 : Chess.ValidBoard := Chess.Board.Board(pieces:=piece5);
expect Chess.BoardIsValid(validBoard0), "If this check fails at runtime, the test does not meet the type constraints";
expect Chess.BoardPreset(validBoard0), "If this check fails at runtime, the test does not meet the preconditions";
Chess.Describe(validBoard0);
}
method {:test} Test41() {
var color0 : Chess.Color := Chess.Color.White;
var kind0 : Chess.Kind := Chess.Kind.King(c:=color0);
var chessPos0 : Chess.ChessPos := Chess.Pos.Pos(row:=0,col:=7);
expect 0 <= chessPos0.row < 8 && 0 <= chessPos0.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece0 : Chess.Piece := Chess.Piece.Piece(kind:=kind0,at:=chessPos0);
var color1 : Chess.Color := Chess.Color.Black;
var kind1 : Chess.Kind := Chess.Kind.Knight(c:=color1);
var chessPos1 : Chess.ChessPos := Chess.Pos.Pos(row:=2,col:=6);
expect 0 <= chessPos1.row < 8 && 0 <= chessPos1.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece1 : Chess.Piece := Chess.Piece.Piece(kind:=kind1,at:=chessPos1);
var color2 : Chess.Color := Chess.Color.Black;
var kind2 : Chess.Kind := Chess.Kind.Knight(c:=color2);
var chessPos2 : Chess.ChessPos := Chess.Pos.Pos(row:=1,col:=4);
expect 0 <= chessPos2.row < 8 && 0 <= chessPos2.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece2 : Chess.Piece := Chess.Piece.Piece(kind:=kind2,at:=chessPos2);
var color3 : Chess.Color := Chess.Color.Black;
var kind3 : Chess.Kind := Chess.Kind.Pawn(c:=color3);
var chessPos3 : Chess.ChessPos := Chess.Pos.Pos(row:=0,col:=5);
expect 0 <= chessPos3.row < 8 && 0 <= chessPos3.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece3 : Chess.Piece := Chess.Piece.Piece(kind:=kind3,at:=chessPos3);
var color4 : Chess.Color := Chess.Color.Black;
var kind4 : Chess.Kind := Chess.Kind.Pawn(c:=color4);
var chessPos4 : Chess.ChessPos := Chess.Pos.Pos(row:=0,col:=6);
expect 0 <= chessPos4.row < 8 && 0 <= chessPos4.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece4 : Chess.Piece := Chess.Piece.Piece(kind:=kind4,at:=chessPos4);
var piece5 : seq<Chess.Piece> := [piece0, piece1, piece2, piece3, piece4];
var validBoard0 : Chess.ValidBoard := Chess.Board.Board(pieces:=piece5);
expect Chess.BoardIsValid(validBoard0), "If this check fails at runtime, the test does not meet the type constraints";
expect Chess.BoardPreset(validBoard0), "If this check fails at runtime, the test does not meet the preconditions";
Chess.Describe(validBoard0);
}
method {:test} Test42() {
var color0 : Chess.Color := Chess.Color.White;
var kind0 : Chess.Kind := Chess.Kind.King(c:=color0);
var chessPos0 : Chess.ChessPos := Chess.Pos.Pos(row:=3,col:=5);
expect 0 <= chessPos0.row < 8 && 0 <= chessPos0.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece0 : Chess.Piece := Chess.Piece.Piece(kind:=kind0,at:=chessPos0);
var color1 : Chess.Color := Chess.Color.Black;
var kind1 : Chess.Kind := Chess.Kind.Knight(c:=color1);
var chessPos1 : Chess.ChessPos := Chess.Pos.Pos(row:=2,col:=3);
expect 0 <= chessPos1.row < 8 && 0 <= chessPos1.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece1 : Chess.Piece := Chess.Piece.Piece(kind:=kind1,at:=chessPos1);
var color2 : Chess.Color := Chess.Color.Black;
var kind2 : Chess.Kind := Chess.Kind.Knight(c:=color2);
var chessPos2 : Chess.ChessPos := Chess.Pos.Pos(row:=1,col:=2);
expect 0 <= chessPos2.row < 8 && 0 <= chessPos2.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece2 : Chess.Piece := Chess.Piece.Piece(kind:=kind2,at:=chessPos2);
var color3 : Chess.Color := Chess.Color.Black;
var kind3 : Chess.Kind := Chess.Kind.Pawn(c:=color3);
var chessPos3 : Chess.ChessPos := Chess.Pos.Pos(row:=5,col:=1);
expect 0 <= chessPos3.row < 8 && 0 <= chessPos3.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece3 : Chess.Piece := Chess.Piece.Piece(kind:=kind3,at:=chessPos3);
var color4 : Chess.Color := Chess.Color.Black;
var kind4 : Chess.Kind := Chess.Kind.Pawn(c:=color4);
var chessPos4 : Chess.ChessPos := Chess.Pos.Pos(row:=4,col:=0);
expect 0 <= chessPos4.row < 8 && 0 <= chessPos4.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece4 : Chess.Piece := Chess.Piece.Piece(kind:=kind4,at:=chessPos4);
var piece5 : seq<Chess.Piece> := [piece0, piece1, piece2, piece3, piece4];
var validBoard0 : Chess.ValidBoard := Chess.Board.Board(pieces:=piece5);
expect Chess.BoardIsValid(validBoard0), "If this check fails at runtime, the test does not meet the type constraints";
expect Chess.BoardPreset(validBoard0), "If this check fails at runtime, the test does not meet the preconditions";
Chess.Describe(validBoard0);
}
method {:test} Test43() {
var color0 : Chess.Color := Chess.Color.White;
var kind0 : Chess.Kind := Chess.Kind.King(c:=color0);
var chessPos0 : Chess.ChessPos := Chess.Pos.Pos(row:=5,col:=5);
expect 0 <= chessPos0.row < 8 && 0 <= chessPos0.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece0 : Chess.Piece := Chess.Piece.Piece(kind:=kind0,at:=chessPos0);
var color1 : Chess.Color := Chess.Color.Black;
var kind1 : Chess.Kind := Chess.Kind.Knight(c:=color1);
var chessPos1 : Chess.ChessPos := Chess.Pos.Pos(row:=7,col:=4);
expect 0 <= chessPos1.row < 8 && 0 <= chessPos1.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece1 : Chess.Piece := Chess.Piece.Piece(kind:=kind1,at:=chessPos1);
var color2 : Chess.Color := Chess.Color.Black;
var kind2 : Chess.Kind := Chess.Kind.Knight(c:=color2);
var chessPos2 : Chess.ChessPos := Chess.Pos.Pos(row:=4,col:=3);
expect 0 <= chessPos2.row < 8 && 0 <= chessPos2.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece2 : Chess.Piece := Chess.Piece.Piece(kind:=kind2,at:=chessPos2);
var color3 : Chess.Color := Chess.Color.Black;
var kind3 : Chess.Kind := Chess.Kind.Pawn(c:=color3);
var chessPos3 : Chess.ChessPos := Chess.Pos.Pos(row:=3,col:=1);
expect 0 <= chessPos3.row < 8 && 0 <= chessPos3.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece3 : Chess.Piece := Chess.Piece.Piece(kind:=kind3,at:=chessPos3);
var color4 : Chess.Color := Chess.Color.Black;
var kind4 : Chess.Kind := Chess.Kind.Pawn(c:=color4);
var chessPos4 : Chess.ChessPos := Chess.Pos.Pos(row:=1,col:=0);
expect 0 <= chessPos4.row < 8 && 0 <= chessPos4.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece4 : Chess.Piece := Chess.Piece.Piece(kind:=kind4,at:=chessPos4);
var piece5 : seq<Chess.Piece> := [piece0, piece1, piece2, piece3, piece4];
var validBoard0 : Chess.ValidBoard := Chess.Board.Board(pieces:=piece5);
expect Chess.BoardIsValid(validBoard0), "If this check fails at runtime, the test does not meet the type constraints";
expect Chess.BoardPreset(validBoard0), "If this check fails at runtime, the test does not meet the preconditions";
Chess.Describe(validBoard0);
}
method {:test} Test44() {
var color0 : Chess.Color := Chess.Color.White;
var kind0 : Chess.Kind := Chess.Kind.King(c:=color0);
var chessPos0 : Chess.ChessPos := Chess.Pos.Pos(row:=0,col:=7);
expect 0 <= chessPos0.row < 8 && 0 <= chessPos0.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece0 : Chess.Piece := Chess.Piece.Piece(kind:=kind0,at:=chessPos0);
var color1 : Chess.Color := Chess.Color.Black;
var kind1 : Chess.Kind := Chess.Kind.Knight(c:=color1);
var chessPos1 : Chess.ChessPos := Chess.Pos.Pos(row:=1,col:=4);
expect 0 <= chessPos1.row < 8 && 0 <= chessPos1.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece1 : Chess.Piece := Chess.Piece.Piece(kind:=kind1,at:=chessPos1);
var color2 : Chess.Color := Chess.Color.Black;
var kind2 : Chess.Kind := Chess.Kind.Knight(c:=color2);
var chessPos2 : Chess.ChessPos := Chess.Pos.Pos(row:=2,col:=6);
expect 0 <= chessPos2.row < 8 && 0 <= chessPos2.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece2 : Chess.Piece := Chess.Piece.Piece(kind:=kind2,at:=chessPos2);
var color3 : Chess.Color := Chess.Color.Black;
var kind3 : Chess.Kind := Chess.Kind.Pawn(c:=color3);
var chessPos3 : Chess.ChessPos := Chess.Pos.Pos(row:=0,col:=5);
expect 0 <= chessPos3.row < 8 && 0 <= chessPos3.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece3 : Chess.Piece := Chess.Piece.Piece(kind:=kind3,at:=chessPos3);
var color4 : Chess.Color := Chess.Color.Black;
var kind4 : Chess.Kind := Chess.Kind.Pawn(c:=color4);
var chessPos4 : Chess.ChessPos := Chess.Pos.Pos(row:=0,col:=6);
expect 0 <= chessPos4.row < 8 && 0 <= chessPos4.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece4 : Chess.Piece := Chess.Piece.Piece(kind:=kind4,at:=chessPos4);
var piece5 : seq<Chess.Piece> := [piece0, piece1, piece2, piece3, piece4];
var validBoard0 : Chess.ValidBoard := Chess.Board.Board(pieces:=piece5);
expect Chess.BoardIsValid(validBoard0), "If this check fails at runtime, the test does not meet the type constraints";
expect Chess.BoardPreset(validBoard0), "If this check fails at runtime, the test does not meet the preconditions";
Chess.Describe(validBoard0);
}
method {:test} Test45() {
var color0 : Chess.Color := Chess.Color.White;
var kind0 : Chess.Kind := Chess.Kind.King(c:=color0);
var chessPos0 : Chess.ChessPos := Chess.Pos.Pos(row:=4,col:=4);
expect 0 <= chessPos0.row < 8 && 0 <= chessPos0.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece0 : Chess.Piece := Chess.Piece.Piece(kind:=kind0,at:=chessPos0);
var color1 : Chess.Color := Chess.Color.Black;
var kind1 : Chess.Kind := Chess.Kind.Knight(c:=color1);
var chessPos1 : Chess.ChessPos := Chess.Pos.Pos(row:=6,col:=3);
expect 0 <= chessPos1.row < 8 && 0 <= chessPos1.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece1 : Chess.Piece := Chess.Piece.Piece(kind:=kind1,at:=chessPos1);
var color2 : Chess.Color := Chess.Color.Black;
var kind2 : Chess.Kind := Chess.Kind.Knight(c:=color2);
var chessPos2 : Chess.ChessPos := Chess.Pos.Pos(row:=3,col:=2);
expect 0 <= chessPos2.row < 8 && 0 <= chessPos2.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece2 : Chess.Piece := Chess.Piece.Piece(kind:=kind2,at:=chessPos2);
var color3 : Chess.Color := Chess.Color.Black;
var kind3 : Chess.Kind := Chess.Kind.Pawn(c:=color3);
var chessPos3 : Chess.ChessPos := Chess.Pos.Pos(row:=1,col:=1);
expect 0 <= chessPos3.row < 8 && 0 <= chessPos3.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece3 : Chess.Piece := Chess.Piece.Piece(kind:=kind3,at:=chessPos3);
var color4 : Chess.Color := Chess.Color.Black;
var kind4 : Chess.Kind := Chess.Kind.Pawn(c:=color4);
var chessPos4 : Chess.ChessPos := Chess.Pos.Pos(row:=0,col:=0);
expect 0 <= chessPos4.row < 8 && 0 <= chessPos4.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece4 : Chess.Piece := Chess.Piece.Piece(kind:=kind4,at:=chessPos4);
var piece5 : seq<Chess.Piece> := [piece0, piece1, piece2, piece3, piece4];
var validBoard0 : Chess.ValidBoard := Chess.Board.Board(pieces:=piece5);
expect Chess.BoardIsValid(validBoard0), "If this check fails at runtime, the test does not meet the type constraints";
expect Chess.BoardPreset(validBoard0), "If this check fails at runtime, the test does not meet the preconditions";
Chess.Describe(validBoard0);
}
method {:test} Test49() {
var color0 : Chess.Color := Chess.Color.White;
var kind0 : Chess.Kind := Chess.Kind.King(c:=color0);
var chessPos0 : Chess.ChessPos := Chess.Pos.Pos(row:=3,col:=5);
expect 0 <= chessPos0.row < 8 && 0 <= chessPos0.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece0 : Chess.Piece := Chess.Piece.Piece(kind:=kind0,at:=chessPos0);
var color1 : Chess.Color := Chess.Color.Black;
var kind1 : Chess.Kind := Chess.Kind.Knight(c:=color1);
var chessPos1 : Chess.ChessPos := Chess.Pos.Pos(row:=5,col:=4);
expect 0 <= chessPos1.row < 8 && 0 <= chessPos1.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece1 : Chess.Piece := Chess.Piece.Piece(kind:=kind1,at:=chessPos1);
var color2 : Chess.Color := Chess.Color.Black;
var kind2 : Chess.Kind := Chess.Kind.Knight(c:=color2);
var chessPos2 : Chess.ChessPos := Chess.Pos.Pos(row:=2,col:=3);
expect 0 <= chessPos2.row < 8 && 0 <= chessPos2.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece2 : Chess.Piece := Chess.Piece.Piece(kind:=kind2,at:=chessPos2);
var color3 : Chess.Color := Chess.Color.Black;
var kind3 : Chess.Kind := Chess.Kind.Pawn(c:=color3);
var chessPos3 : Chess.ChessPos := Chess.Pos.Pos(row:=1,col:=1);
expect 0 <= chessPos3.row < 8 && 0 <= chessPos3.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece3 : Chess.Piece := Chess.Piece.Piece(kind:=kind3,at:=chessPos3);
var color4 : Chess.Color := Chess.Color.Black;
var kind4 : Chess.Kind := Chess.Kind.Pawn(c:=color4);
var chessPos4 : Chess.ChessPos := Chess.Pos.Pos(row:=0,col:=0);
expect 0 <= chessPos4.row < 8 && 0 <= chessPos4.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece4 : Chess.Piece := Chess.Piece.Piece(kind:=kind4,at:=chessPos4);
var piece5 : seq<Chess.Piece> := [piece0, piece1, piece2, piece3, piece4];
var validBoard0 : Chess.ValidBoard := Chess.Board.Board(pieces:=piece5);
expect Chess.BoardIsValid(validBoard0), "If this check fails at runtime, the test does not meet the type constraints";
expect Chess.BoardPreset(validBoard0), "If this check fails at runtime, the test does not meet the preconditions";
Chess.Describe(validBoard0);
}
method {:test} Test55() {
var color0 : Chess.Color := Chess.Color.White;
var kind0 : Chess.Kind := Chess.Kind.King(c:=color0);
var chessPos0 : Chess.ChessPos := Chess.Pos.Pos(row:=5,col:=5);
expect 0 <= chessPos0.row < 8 && 0 <= chessPos0.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece0 : Chess.Piece := Chess.Piece.Piece(kind:=kind0,at:=chessPos0);
var color1 : Chess.Color := Chess.Color.Black;
var kind1 : Chess.Kind := Chess.Kind.Knight(c:=color1);
var chessPos1 : Chess.ChessPos := Chess.Pos.Pos(row:=7,col:=4);
expect 0 <= chessPos1.row < 8 && 0 <= chessPos1.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece1 : Chess.Piece := Chess.Piece.Piece(kind:=kind1,at:=chessPos1);
var color2 : Chess.Color := Chess.Color.Black;
var kind2 : Chess.Kind := Chess.Kind.Knight(c:=color2);
var chessPos2 : Chess.ChessPos := Chess.Pos.Pos(row:=4,col:=3);
expect 0 <= chessPos2.row < 8 && 0 <= chessPos2.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece2 : Chess.Piece := Chess.Piece.Piece(kind:=kind2,at:=chessPos2);
var color3 : Chess.Color := Chess.Color.Black;
var kind3 : Chess.Kind := Chess.Kind.Pawn(c:=color3);
var chessPos3 : Chess.ChessPos := Chess.Pos.Pos(row:=2,col:=2);
expect 0 <= chessPos3.row < 8 && 0 <= chessPos3.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece3 : Chess.Piece := Chess.Piece.Piece(kind:=kind3,at:=chessPos3);
var color4 : Chess.Color := Chess.Color.Black;
var kind4 : Chess.Kind := Chess.Kind.Pawn(c:=color4);
var chessPos4 : Chess.ChessPos := Chess.Pos.Pos(row:=0,col:=1);
expect 0 <= chessPos4.row < 8 && 0 <= chessPos4.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece4 : Chess.Piece := Chess.Piece.Piece(kind:=kind4,at:=chessPos4);
var piece5 : seq<Chess.Piece> := [piece0, piece1, piece2, piece3, piece4];
var validBoard0 : Chess.ValidBoard := Chess.Board.Board(pieces:=piece5);
expect Chess.BoardIsValid(validBoard0), "If this check fails at runtime, the test does not meet the type constraints";
expect Chess.BoardPreset(validBoard0), "If this check fails at runtime, the test does not meet the preconditions";
Chess.Describe(validBoard0);
}
method {:test} Test57() {
var color0 : Chess.Color := Chess.Color.White;
var kind0 : Chess.Kind := Chess.Kind.King(c:=color0);
var chessPos0 : Chess.ChessPos := Chess.Pos.Pos(row:=5,col:=6);
expect 0 <= chessPos0.row < 8 && 0 <= chessPos0.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece0 : Chess.Piece := Chess.Piece.Piece(kind:=kind0,at:=chessPos0);
var color1 : Chess.Color := Chess.Color.Black;
var kind1 : Chess.Kind := Chess.Kind.Knight(c:=color1);
var chessPos1 : Chess.ChessPos := Chess.Pos.Pos(row:=7,col:=5);
expect 0 <= chessPos1.row < 8 && 0 <= chessPos1.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece1 : Chess.Piece := Chess.Piece.Piece(kind:=kind1,at:=chessPos1);
var color2 : Chess.Color := Chess.Color.Black;
var kind2 : Chess.Kind := Chess.Kind.Knight(c:=color2);
var chessPos2 : Chess.ChessPos := Chess.Pos.Pos(row:=4,col:=4);
expect 0 <= chessPos2.row < 8 && 0 <= chessPos2.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece2 : Chess.Piece := Chess.Piece.Piece(kind:=kind2,at:=chessPos2);
var color3 : Chess.Color := Chess.Color.Black;
var kind3 : Chess.Kind := Chess.Kind.Pawn(c:=color3);
var chessPos3 : Chess.ChessPos := Chess.Pos.Pos(row:=3,col:=2);
expect 0 <= chessPos3.row < 8 && 0 <= chessPos3.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece3 : Chess.Piece := Chess.Piece.Piece(kind:=kind3,at:=chessPos3);
var color4 : Chess.Color := Chess.Color.Black;
var kind4 : Chess.Kind := Chess.Kind.Pawn(c:=color4);
var chessPos4 : Chess.ChessPos := Chess.Pos.Pos(row:=1,col:=0);
expect 0 <= chessPos4.row < 8 && 0 <= chessPos4.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece4 : Chess.Piece := Chess.Piece.Piece(kind:=kind4,at:=chessPos4);
var piece5 : seq<Chess.Piece> := [piece0, piece1, piece2, piece3, piece4];
var validBoard0 : Chess.ValidBoard := Chess.Board.Board(pieces:=piece5);
expect Chess.BoardIsValid(validBoard0), "If this check fails at runtime, the test does not meet the type constraints";
expect Chess.BoardPreset(validBoard0), "If this check fails at runtime, the test does not meet the preconditions";
Chess.Describe(validBoard0);
}
method {:test} Test59() {
var color0 : Chess.Color := Chess.Color.White;
var kind0 : Chess.Kind := Chess.Kind.King(c:=color0);
var chessPos0 : Chess.ChessPos := Chess.Pos.Pos(row:=3,col:=6);
expect 0 <= chessPos0.row < 8 && 0 <= chessPos0.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece0 : Chess.Piece := Chess.Piece.Piece(kind:=kind0,at:=chessPos0);
var color1 : Chess.Color := Chess.Color.Black;
var kind1 : Chess.Kind := Chess.Kind.Knight(c:=color1);
var chessPos1 : Chess.ChessPos := Chess.Pos.Pos(row:=2,col:=4);
expect 0 <= chessPos1.row < 8 && 0 <= chessPos1.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece1 : Chess.Piece := Chess.Piece.Piece(kind:=kind1,at:=chessPos1);
var color2 : Chess.Color := Chess.Color.Black;
var kind2 : Chess.Kind := Chess.Kind.Knight(c:=color2);
var chessPos2 : Chess.ChessPos := Chess.Pos.Pos(row:=1,col:=3);
expect 0 <= chessPos2.row < 8 && 0 <= chessPos2.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece2 : Chess.Piece := Chess.Piece.Piece(kind:=kind2,at:=chessPos2);
var color3 : Chess.Color := Chess.Color.Black;
var kind3 : Chess.Kind := Chess.Kind.Pawn(c:=color3);
var chessPos3 : Chess.ChessPos := Chess.Pos.Pos(row:=6,col:=2);
expect 0 <= chessPos3.row < 8 && 0 <= chessPos3.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece3 : Chess.Piece := Chess.Piece.Piece(kind:=kind3,at:=chessPos3);
var color4 : Chess.Color := Chess.Color.Black;
var kind4 : Chess.Kind := Chess.Kind.Pawn(c:=color4);
var chessPos4 : Chess.ChessPos := Chess.Pos.Pos(row:=5,col:=1);
expect 0 <= chessPos4.row < 8 && 0 <= chessPos4.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece4 : Chess.Piece := Chess.Piece.Piece(kind:=kind4,at:=chessPos4);
var piece5 : seq<Chess.Piece> := [piece0, piece1, piece2, piece3, piece4];
var validBoard0 : Chess.ValidBoard := Chess.Board.Board(pieces:=piece5);
expect Chess.BoardIsValid(validBoard0), "If this check fails at runtime, the test does not meet the type constraints";
expect Chess.BoardPreset(validBoard0), "If this check fails at runtime, the test does not meet the preconditions";
Chess.Describe(validBoard0);
}
method {:test} Test60() {
var color0 : Chess.Color := Chess.Color.White;
var kind0 : Chess.Kind := Chess.Kind.King(c:=color0);
var chessPos0 : Chess.ChessPos := Chess.Pos.Pos(row:=5,col:=6);
expect 0 <= chessPos0.row < 8 && 0 <= chessPos0.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece0 : Chess.Piece := Chess.Piece.Piece(kind:=kind0,at:=chessPos0);
var color1 : Chess.Color := Chess.Color.Black;
var kind1 : Chess.Kind := Chess.Kind.Knight(c:=color1);
var chessPos1 : Chess.ChessPos := Chess.Pos.Pos(row:=7,col:=5);
expect 0 <= chessPos1.row < 8 && 0 <= chessPos1.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece1 : Chess.Piece := Chess.Piece.Piece(kind:=kind1,at:=chessPos1);
var color2 : Chess.Color := Chess.Color.Black;
var kind2 : Chess.Kind := Chess.Kind.Knight(c:=color2);
var chessPos2 : Chess.ChessPos := Chess.Pos.Pos(row:=4,col:=4);
expect 0 <= chessPos2.row < 8 && 0 <= chessPos2.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece2 : Chess.Piece := Chess.Piece.Piece(kind:=kind2,at:=chessPos2);
var color3 : Chess.Color := Chess.Color.Black;
var kind3 : Chess.Kind := Chess.Kind.Pawn(c:=color3);
var chessPos3 : Chess.ChessPos := Chess.Pos.Pos(row:=3,col:=3);
expect 0 <= chessPos3.row < 8 && 0 <= chessPos3.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece3 : Chess.Piece := Chess.Piece.Piece(kind:=kind3,at:=chessPos3);
var color4 : Chess.Color := Chess.Color.Black;
var kind4 : Chess.Kind := Chess.Kind.Pawn(c:=color4);
var chessPos4 : Chess.ChessPos := Chess.Pos.Pos(row:=0,col:=0);
expect 0 <= chessPos4.row < 8 && 0 <= chessPos4.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece4 : Chess.Piece := Chess.Piece.Piece(kind:=kind4,at:=chessPos4);
var piece5 : seq<Chess.Piece> := [piece0, piece1, piece2, piece3, piece4];
var validBoard0 : Chess.ValidBoard := Chess.Board.Board(pieces:=piece5);
expect Chess.BoardIsValid(validBoard0), "If this check fails at runtime, the test does not meet the type constraints";
expect Chess.BoardPreset(validBoard0), "If this check fails at runtime, the test does not meet the preconditions";
Chess.Describe(validBoard0);
}
method {:test} Test61() {
var color0 : Chess.Color := Chess.Color.White;
var kind0 : Chess.Kind := Chess.Kind.King(c:=color0);
var chessPos0 : Chess.ChessPos := Chess.Pos.Pos(row:=5,col:=5);
expect 0 <= chessPos0.row < 8 && 0 <= chessPos0.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece0 : Chess.Piece := Chess.Piece.Piece(kind:=kind0,at:=chessPos0);
var color1 : Chess.Color := Chess.Color.Black;
var kind1 : Chess.Kind := Chess.Kind.Knight(c:=color1);
var chessPos1 : Chess.ChessPos := Chess.Pos.Pos(row:=7,col:=4);
expect 0 <= chessPos1.row < 8 && 0 <= chessPos1.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece1 : Chess.Piece := Chess.Piece.Piece(kind:=kind1,at:=chessPos1);
var color2 : Chess.Color := Chess.Color.Black;
var kind2 : Chess.Kind := Chess.Kind.Knight(c:=color2);
var chessPos2 : Chess.ChessPos := Chess.Pos.Pos(row:=4,col:=3);
expect 0 <= chessPos2.row < 8 && 0 <= chessPos2.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece2 : Chess.Piece := Chess.Piece.Piece(kind:=kind2,at:=chessPos2);
var color3 : Chess.Color := Chess.Color.Black;
var kind3 : Chess.Kind := Chess.Kind.Pawn(c:=color3);
var chessPos3 : Chess.ChessPos := Chess.Pos.Pos(row:=1,col:=2);
expect 0 <= chessPos3.row < 8 && 0 <= chessPos3.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece3 : Chess.Piece := Chess.Piece.Piece(kind:=kind3,at:=chessPos3);
var color4 : Chess.Color := Chess.Color.Black;
var kind4 : Chess.Kind := Chess.Kind.Pawn(c:=color4);
var chessPos4 : Chess.ChessPos := Chess.Pos.Pos(row:=0,col:=1);
expect 0 <= chessPos4.row < 8 && 0 <= chessPos4.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece4 : Chess.Piece := Chess.Piece.Piece(kind:=kind4,at:=chessPos4);
var piece5 : seq<Chess.Piece> := [piece0, piece1, piece2, piece3, piece4];
var validBoard0 : Chess.ValidBoard := Chess.Board.Board(pieces:=piece5);
expect Chess.BoardIsValid(validBoard0), "If this check fails at runtime, the test does not meet the type constraints";
expect Chess.BoardPreset(validBoard0), "If this check fails at runtime, the test does not meet the preconditions";
Chess.Describe(validBoard0);
}
method {:test} Test65() {
var color0 : Chess.Color := Chess.Color.White;
var kind0 : Chess.Kind := Chess.Kind.King(c:=color0);
var chessPos0 : Chess.ChessPos := Chess.Pos.Pos(row:=4,col:=6);
expect 0 <= chessPos0.row < 8 && 0 <= chessPos0.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece0 : Chess.Piece := Chess.Piece.Piece(kind:=kind0,at:=chessPos0);
var color1 : Chess.Color := Chess.Color.Black;
var kind1 : Chess.Kind := Chess.Kind.Knight(c:=color1);
var chessPos1 : Chess.ChessPos := Chess.Pos.Pos(row:=6,col:=5);
expect 0 <= chessPos1.row < 8 && 0 <= chessPos1.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece1 : Chess.Piece := Chess.Piece.Piece(kind:=kind1,at:=chessPos1);
var color2 : Chess.Color := Chess.Color.Black;
var kind2 : Chess.Kind := Chess.Kind.Knight(c:=color2);
var chessPos2 : Chess.ChessPos := Chess.Pos.Pos(row:=3,col:=4);
expect 0 <= chessPos2.row < 8 && 0 <= chessPos2.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece2 : Chess.Piece := Chess.Piece.Piece(kind:=kind2,at:=chessPos2);
var color3 : Chess.Color := Chess.Color.Black;
var kind3 : Chess.Kind := Chess.Kind.Pawn(c:=color3);
var chessPos3 : Chess.ChessPos := Chess.Pos.Pos(row:=2,col:=1);
expect 0 <= chessPos3.row < 8 && 0 <= chessPos3.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece3 : Chess.Piece := Chess.Piece.Piece(kind:=kind3,at:=chessPos3);
var color4 : Chess.Color := Chess.Color.Black;
var kind4 : Chess.Kind := Chess.Kind.Pawn(c:=color4);
var chessPos4 : Chess.ChessPos := Chess.Pos.Pos(row:=1,col:=0);
expect 0 <= chessPos4.row < 8 && 0 <= chessPos4.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece4 : Chess.Piece := Chess.Piece.Piece(kind:=kind4,at:=chessPos4);
var piece5 : seq<Chess.Piece> := [piece0, piece1, piece2, piece3, piece4];
var validBoard0 : Chess.ValidBoard := Chess.Board.Board(pieces:=piece5);
expect Chess.BoardIsValid(validBoard0), "If this check fails at runtime, the test does not meet the type constraints";
expect Chess.BoardPreset(validBoard0), "If this check fails at runtime, the test does not meet the preconditions";
Chess.Describe(validBoard0);
}
method {:test} Test67() {
var color0 : Chess.Color := Chess.Color.White;
var kind0 : Chess.Kind := Chess.Kind.King(c:=color0);
var chessPos0 : Chess.ChessPos := Chess.Pos.Pos(row:=7,col:=7);
expect 0 <= chessPos0.row < 8 && 0 <= chessPos0.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece0 : Chess.Piece := Chess.Piece.Piece(kind:=kind0,at:=chessPos0);
var color1 : Chess.Color := Chess.Color.Black;
var kind1 : Chess.Kind := Chess.Kind.Knight(c:=color1);
var chessPos1 : Chess.ChessPos := Chess.Pos.Pos(row:=5,col:=5);
expect 0 <= chessPos1.row < 8 && 0 <= chessPos1.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece1 : Chess.Piece := Chess.Piece.Piece(kind:=kind1,at:=chessPos1);
var color2 : Chess.Color := Chess.Color.Black;
var kind2 : Chess.Kind := Chess.Kind.Knight(c:=color2);
var chessPos2 : Chess.ChessPos := Chess.Pos.Pos(row:=5,col:=4);
expect 0 <= chessPos2.row < 8 && 0 <= chessPos2.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece2 : Chess.Piece := Chess.Piece.Piece(kind:=kind2,at:=chessPos2);
var color3 : Chess.Color := Chess.Color.Black;
var kind3 : Chess.Kind := Chess.Kind.Pawn(c:=color3);
var chessPos3 : Chess.ChessPos := Chess.Pos.Pos(row:=0,col:=3);
expect 0 <= chessPos3.row < 8 && 0 <= chessPos3.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece3 : Chess.Piece := Chess.Piece.Piece(kind:=kind3,at:=chessPos3);
var color4 : Chess.Color := Chess.Color.Black;
var kind4 : Chess.Kind := Chess.Kind.Pawn(c:=color4);
var chessPos4 : Chess.ChessPos := Chess.Pos.Pos(row:=6,col:=6);
expect 0 <= chessPos4.row < 8 && 0 <= chessPos4.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece4 : Chess.Piece := Chess.Piece.Piece(kind:=kind4,at:=chessPos4);
var piece5 : seq<Chess.Piece> := [piece0, piece1, piece2, piece3, piece4];
var validBoard0 : Chess.ValidBoard := Chess.Board.Board(pieces:=piece5);
expect Chess.BoardIsValid(validBoard0), "If this check fails at runtime, the test does not meet the type constraints";
expect Chess.BoardPreset(validBoard0), "If this check fails at runtime, the test does not meet the preconditions";
Chess.Describe(validBoard0);
}
method {:test} Test68() {
var color0 : Chess.Color := Chess.Color.White;
var kind0 : Chess.Kind := Chess.Kind.King(c:=color0);
var chessPos0 : Chess.ChessPos := Chess.Pos.Pos(row:=7,col:=7);
expect 0 <= chessPos0.row < 8 && 0 <= chessPos0.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece0 : Chess.Piece := Chess.Piece.Piece(kind:=kind0,at:=chessPos0);
var color1 : Chess.Color := Chess.Color.Black;
var kind1 : Chess.Kind := Chess.Kind.Knight(c:=color1);
var chessPos1 : Chess.ChessPos := Chess.Pos.Pos(row:=5,col:=5);
expect 0 <= chessPos1.row < 8 && 0 <= chessPos1.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece1 : Chess.Piece := Chess.Piece.Piece(kind:=kind1,at:=chessPos1);
var color2 : Chess.Color := Chess.Color.Black;
var kind2 : Chess.Kind := Chess.Kind.Knight(c:=color2);
var chessPos2 : Chess.ChessPos := Chess.Pos.Pos(row:=5,col:=4);
expect 0 <= chessPos2.row < 8 && 0 <= chessPos2.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece2 : Chess.Piece := Chess.Piece.Piece(kind:=kind2,at:=chessPos2);
var color3 : Chess.Color := Chess.Color.Black;
var kind3 : Chess.Kind := Chess.Kind.Pawn(c:=color3);
var chessPos3 : Chess.ChessPos := Chess.Pos.Pos(row:=4,col:=3);
expect 0 <= chessPos3.row < 8 && 0 <= chessPos3.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece3 : Chess.Piece := Chess.Piece.Piece(kind:=kind3,at:=chessPos3);
var color4 : Chess.Color := Chess.Color.Black;
var kind4 : Chess.Kind := Chess.Kind.Pawn(c:=color4);
var chessPos4 : Chess.ChessPos := Chess.Pos.Pos(row:=6,col:=6);
expect 0 <= chessPos4.row < 8 && 0 <= chessPos4.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece4 : Chess.Piece := Chess.Piece.Piece(kind:=kind4,at:=chessPos4);
var piece5 : seq<Chess.Piece> := [piece0, piece1, piece2, piece3, piece4];
var validBoard0 : Chess.ValidBoard := Chess.Board.Board(pieces:=piece5);
expect Chess.BoardIsValid(validBoard0), "If this check fails at runtime, the test does not meet the type constraints";
expect Chess.BoardPreset(validBoard0), "If this check fails at runtime, the test does not meet the preconditions";
Chess.Describe(validBoard0);
}
method {:test} Test74() {
var color0 : Chess.Color := Chess.Color.White;
var kind0 : Chess.Kind := Chess.Kind.King(c:=color0);
var chessPos0 : Chess.ChessPos := Chess.Pos.Pos(row:=7,col:=7);
expect 0 <= chessPos0.row < 8 && 0 <= chessPos0.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece0 : Chess.Piece := Chess.Piece.Piece(kind:=kind0,at:=chessPos0);
var color1 : Chess.Color := Chess.Color.Black;
var kind1 : Chess.Kind := Chess.Kind.Knight(c:=color1);
var chessPos1 : Chess.ChessPos := Chess.Pos.Pos(row:=5,col:=5);
expect 0 <= chessPos1.row < 8 && 0 <= chessPos1.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece1 : Chess.Piece := Chess.Piece.Piece(kind:=kind1,at:=chessPos1);
var color2 : Chess.Color := Chess.Color.Black;
var kind2 : Chess.Kind := Chess.Kind.Knight(c:=color2);
var chessPos2 : Chess.ChessPos := Chess.Pos.Pos(row:=5,col:=4);
expect 0 <= chessPos2.row < 8 && 0 <= chessPos2.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece2 : Chess.Piece := Chess.Piece.Piece(kind:=kind2,at:=chessPos2);
var color3 : Chess.Color := Chess.Color.Black;
var kind3 : Chess.Kind := Chess.Kind.Pawn(c:=color3);
var chessPos3 : Chess.ChessPos := Chess.Pos.Pos(row:=3,col:=3);
expect 0 <= chessPos3.row < 8 && 0 <= chessPos3.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece3 : Chess.Piece := Chess.Piece.Piece(kind:=kind3,at:=chessPos3);
var color4 : Chess.Color := Chess.Color.Black;
var kind4 : Chess.Kind := Chess.Kind.Pawn(c:=color4);
var chessPos4 : Chess.ChessPos := Chess.Pos.Pos(row:=6,col:=6);
expect 0 <= chessPos4.row < 8 && 0 <= chessPos4.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece4 : Chess.Piece := Chess.Piece.Piece(kind:=kind4,at:=chessPos4);
var piece5 : seq<Chess.Piece> := [piece0, piece1, piece2, piece3, piece4];
var validBoard0 : Chess.ValidBoard := Chess.Board.Board(pieces:=piece5);
expect Chess.BoardIsValid(validBoard0), "If this check fails at runtime, the test does not meet the type constraints";
expect Chess.BoardPreset(validBoard0), "If this check fails at runtime, the test does not meet the preconditions";
Chess.Describe(validBoard0);
}
method {:test} Test79() {
var color0 : Chess.Color := Chess.Color.White;
var kind0 : Chess.Kind := Chess.Kind.King(c:=color0);
var chessPos0 : Chess.ChessPos := Chess.Pos.Pos(row:=7,col:=6);
expect 0 <= chessPos0.row < 8 && 0 <= chessPos0.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece0 : Chess.Piece := Chess.Piece.Piece(kind:=kind0,at:=chessPos0);
var color1 : Chess.Color := Chess.Color.Black;
var kind1 : Chess.Kind := Chess.Kind.Knight(c:=color1);
var chessPos1 : Chess.ChessPos := Chess.Pos.Pos(row:=2,col:=3);
expect 0 <= chessPos1.row < 8 && 0 <= chessPos1.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece1 : Chess.Piece := Chess.Piece.Piece(kind:=kind1,at:=chessPos1);
var color2 : Chess.Color := Chess.Color.Black;
var kind2 : Chess.Kind := Chess.Kind.Knight(c:=color2);
var chessPos2 : Chess.ChessPos := Chess.Pos.Pos(row:=0,col:=2);
expect 0 <= chessPos2.row < 8 && 0 <= chessPos2.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece2 : Chess.Piece := Chess.Piece.Piece(kind:=kind2,at:=chessPos2);
var color3 : Chess.Color := Chess.Color.Black;
var kind3 : Chess.Kind := Chess.Kind.Pawn(c:=color3);
var chessPos3 : Chess.ChessPos := Chess.Pos.Pos(row:=6,col:=7);
expect 0 <= chessPos3.row < 8 && 0 <= chessPos3.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece3 : Chess.Piece := Chess.Piece.Piece(kind:=kind3,at:=chessPos3);
var color4 : Chess.Color := Chess.Color.Black;
var kind4 : Chess.Kind := Chess.Kind.Pawn(c:=color4);
var chessPos4 : Chess.ChessPos := Chess.Pos.Pos(row:=6,col:=5);
expect 0 <= chessPos4.row < 8 && 0 <= chessPos4.col < 8, "If this check fails at runtime, the test does not meet the type constraints";
var piece4 : Chess.Piece := Chess.Piece.Piece(kind:=kind4,at:=chessPos4);
var piece5 : seq<Chess.Piece> := [piece0, piece1, piece2, piece3, piece4];
var validBoard0 : Chess.ValidBoard := Chess.Board.Board(pieces:=piece5);
expect Chess.BoardIsValid(validBoard0), "If this check fails at runtime, the test does not meet the type constraints";
expect Chess.BoardPreset(validBoard0), "If this check fails at runtime, the test does not meet the preconditions";
Chess.Describe(validBoard0);
}

}
